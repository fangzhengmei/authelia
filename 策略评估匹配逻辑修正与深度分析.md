# Authelia 访问主体与组规则匹配逻辑修正与深度分析

## 一、MatchesSubjects 匿名用户处理的关键逻辑错误

### 1.1 问题代码定位

`internal/authorization/access_control_rule.go:149-155`：

```go
func (acr *AccessControlRule) MatchesSubjects(subject Subject) (match bool) {
    if subject.IsAnonymous() {
        return true
    }

    return acr.MatchesSubjectExact(subject)
}
```

`IsAnonymous` 定义于 `internal/authorization/types.go:62-64`：

```go
func (s Subject) IsAnonymous() bool {
    return s.Username == "" && len(s.Groups) == 0
}
```

### 1.2 逻辑错误分析

**核心问题**：当 `Subject.IsAnonymous()` 为 `true` 时，`MatchesSubjects` 无条件返回 `true`，**完全跳过了主体约束检查**。这意味着：一条配置了 `subject: ["group:admins"]` 的 deny 规则，对匿名用户也会在主体维度上匹配成功。

**实际影响链路**：

```
匿名用户请求 → Subject{Username:"", Groups:[]}
    → IsAnonymous() = true
    → MatchesSubjects() = true （直接返回，跳过主体约束！）
    → IsMatch() 所有条件可能全部通过 → 规则命中 → 策略生效
```

但这个"错误"被下游 `isAuthzResult` 部分补偿了（见第三节），形成了一个**双层逻辑耦合**的设计：上层故意放宽匿名匹配，下层通过 `ruleHasSubject` 标记和 `isAuthzResult` 做二次判定。

### 1.3 修正理解：这是一个有意为之的两阶段设计

深入分析 `handler_authz.go:192-200` 和 `handler_authz_util.go:28-43` 后，可以确认这不是 bug，而是 Authelia 的**两阶段判定架构**：

| 阶段 | 位置 | 职责 |
|------|------|------|
| 第一阶段：规则匹配 | `MatchesSubjects` | 放宽匿名用户主体约束，让带主体约束的规则也能"命中"匿名用户 |
| 第二阶段：结果判定 | `isAuthzResult` | 结合 `ruleHasSubject` 标记区分匿名/已认证用户的拒绝语义 |

**设计意图**：对匿名用户来说，如果某条 deny 规则包含 `subject: ["group:admins"]`，匿名用户在第一阶段能命中该规则（返回 Denied），但第二阶段会因为用户匿名且规则有主体约束，将结果从 Forbidden 降级为 Unauthorized（提示登录而非直接拒绝）。这样匿名用户有机会通过登录获取身份后重新评估。

### 1.4 潜在风险

尽管是有意设计，但仍存在语义上的不精确：

1. **统计误判**：对于 `one_factor` 或 `two_factor` 策略的规则，匿名用户命中后会走 `AuthzResultUnauthorized`，但如果规则同时包含 `subject: ["group:admins"]`，匿名用户**永远无法满足**该主体约束，返回 Unauthorized（暗示"请登录"）具有误导性——即使用户登录后不属于 admins 组，依然会被拒绝。

2. **ruleHasSubject 语义不完整**：`HasSubjects` 标记**不仅来自 `Subjects` 字段**，还来自域名和资源中的 `{user}` / `{group}` 占位符（见第四节），导致一个没有显式 `subject` 配置但包含 `{user}.example.com` 的规则也会被标记为 `HasSubjects = true`。

---

## 二、身份提供方属性到组判定的完整执行流程

### 2.1 两条独立的组获取路径

Authelia 有两条截然不同的组获取路径，取决于认证策略类型：

#### 路径 A：Cookie 会话策略（`CookieSessionAuthnStrategy`）

```
CookieSessionAuthnStrategy.Get()
    ↓
manager.GetSession() → 获取会话中的 userSession
    ↓
handleAuthnCookieValidate()
    ↓
handleSessionValidateRefresh()  ← 按 refresh_interval 周期刷新
    ↓
UserProvider.GetDetails(username)  ← 从后端实时获取
    ↓
userSession.Groups = details.Groups  ← 更新会话中的组信息
    ↓
返回 Authn{Details: authentication.UserDetails{Groups: userSession.Groups}}
```

**关键**：Cookie 策略下，组信息来自**会话缓存 + 周期性刷新**，`refresh_interval` 默认 5 分钟。在此间隔内，LDAP/文件中的组变更不会反映到策略评估中。

#### 路径 B：Header 认证策略（`HeaderAuthnStrategy` / `HeaderLegacyAuthnStrategy`）

```
HeaderAuthnStrategy.Get()
    ↓
解析 Authorization 头 → 获取 username
    ↓
UserProvider.GetDetails(username)  ← 每次请求都实时获取！
    ↓
返回 Authn{Details: *details}  ← Groups 直接来自后端
```

**关键**：Header 策略下，组信息是**每次请求实时获取**的，不存在缓存延迟。

### 2.2 LDAP 后端组获取详细流程

`internal/authentication/ldap_user_provider.go:118-153`：

```
LDAPUserProvider.GetDetails(username)
    ↓
1. getUserProfile(client, username)
    ↓ LDAP Search (usersBaseDN + usersFilter)
    ↓ 提取属性: Username, DisplayName, Emails, MemberOf
    ↓
2. getUserGroups(client, username, profile)
    ↓ 根据 GroupSearchMode 选择分支:
    │
    ├─ "filter" 模式（默认）:
    │   getUserGroupsRequestFilter()
    │   ↓ LDAP Search (groupsBaseDN + groupsFilter)
    │   ↓ 遍历结果，提取 GroupName 属性
    │   ↓
    │   → groups = [GroupName from each entry]
    │
    └─ "memberof" 模式:
        getUserGroupsRequestMemberOf()
        ↓ LDAP Search (groupsBaseDN + groupsFilter)
        ↓ 遍历结果，双重验证:
        │   1. entry.DN 必须在 profile.MemberOf 列表中
        │   2. 提取 GroupName 属性
        ↓
        → groups = [GroupName where DN ∈ profile.MemberOf]
    ↓
3. 返回 UserDetails{Username, DisplayName, Emails, Groups}
```

**属性映射关系**（`internal/configuration/schema/authentication.go:165-192`）：

| Authelia 内部字段 | LDAP 配置属性 | Active Directory 默认 | RFC2307bis 默认 |
|---|---|---|---|
| Username | `attributes.username` | `sAMAccountName` | `uid` |
| Groups（来源1） | `attributes.member_of` | `memberOf` | `memberOf` |
| Groups（来源2） | `attributes.group_name` | `cn` | `cn` |
| GroupsFilter | `groups_filter` | `(&(member={dn})(...))` | `(&(|(member={dn})(uniqueMember={dn}))(...))` |

**filter 模式与 memberof 模式的区别**：
- **filter 模式**：仅通过 `groups_filter` 搜索，依赖 LDAP 服务器正确实现 `member` 属性的反向引用
- **memberof 模式**：先搜索所有组，再用用户条目的 `memberOf` 属性做交集过滤，适用于 `memberOf` 被可靠维护的场景

### 2.3 文件后端组获取流程

`internal/authentication/file_user_provider.go:100-112`：

```
FileUserProvider.GetDetails(username)
    ↓
FileUserDatabase.GetUserDetails(username)
    ↓ RLock()  [读锁]
    ↓ 查找用户（支持大小写不敏感和邮箱搜索别名）
    ↓
返回 FileUserDatabaseUserDetails{Groups: m.Groups}
    ↓
ToUserDetails() → UserDetails{Groups: m.Groups}
```

**组定义位置**：YAML 文件中每个用户条目的 `groups` 字段：

```yaml
users:
  john:
    displayname: John Doe
    password: ...
    email: john@example.com
    groups:
      - admins
      - developers
```

### 2.4 从 UserDetails 到 Subject 的组传递

`internal/handlers/handler_authz.go:192-200`：

```go
ruleHasSubject, required := ctx.GetProviders().Authorizer.GetRequiredLevel(
    authorization.Subject{
        Username: authn.Details.Username,  // UserDetails.Username
        Groups:   authn.Details.Groups,    // UserDetails.Groups → Subject.Groups
        ClientID: authn.ClientID,
        IP:       ctx.RemoteIP(),
    },
    object,
)
```

**完整的组数据传递链**：

```
LDAP/File 后端
    ↓ 属性映射 (memberOf/cn → Groups)
UserDetails.Groups
    ↓ Cookie策略: 会话缓存 + 周期刷新; Header策略: 实时获取
authn.Details.Groups
    ↓ 直接赋值
Subject.Groups
    ↓ 传递到匹配器
AccessControlGroup.IsMatch(subject)
    ↓ utils.IsStringInSlice(acg.Name, subject.Groups)
判定用户是否属于规则要求的组
```

### 2.5 组判定的时效性差异

| 场景 | 组信息来源 | 数据时效性 | 最大延迟 |
|---|---|---|---|
| Cookie 策略 + LDAP 后端 | 会话缓存 + 周期刷新 | 近实时 | `refresh_interval`（默认5分钟） |
| Cookie 策略 + File 后端 | 会话缓存 + 周期刷新 | 近实时 | `refresh_interval` + 文件重载冷却 |
| Header Basic 策略 + LDAP | 每次请求实时查询 | 实时 | 无 |
| Header Basic 策略 + File | 每次请求实时查询 | 近实时 | 取决于文件最后重载时间 |
| Header Bearer 策略 | OIDC Token 中的 claims | Token 签发时快照 | Token 有效期 |

---

## 三、isAuthzResult 中 Denied + 匿名 + ruleHasSubject 的边界条件分析

### 3.1 isAuthzResult 完整逻辑

`internal/handlers/handler_authz_util.go:28-43`：

```go
func isAuthzResult(level authentication.Level, required authorization.Level, ruleHasSubject bool) AuthzResult {
    switch {
    case required == authorization.Bypass:
        return AuthzResultAuthorized
    case required == authorization.Denied && (level != authentication.NotAuthenticated || !ruleHasSubject):
        return AuthzResultForbidden
    case required == authorization.OneFactor && level >= authentication.OneFactor,
         required == authorization.TwoFactor && level >= authentication.TwoFactor:
        return AuthzResultAuthorized
    default:
        return AuthzResultUnauthorized
    }
}
```

### 3.2 四种 Denied + 匿名/已认证 × 有/无主体约束的场景矩阵

| 场景 | `level` | `ruleHasSubject` | 条件表达式 | 结果 |
|---|---|---|---|---|
| 已认证 + 规则有主体约束 | `OneFactor` | `true` | `Denied && (true \|\| false)` = `true` | **Forbidden** |
| 已认证 + 规则无主体约束 | `OneFactor` | `false` | `Denied && (true \|\| true)` = `true` | **Forbidden** |
| 匿名 + 规则有主体约束 | `NotAuthenticated` | `true` | `Denied && (false \|\| false)` = `false` | **Unauthorized** ← 进入 default |
| 匿名 + 规则无主体约束 | `NotAuthenticated` | `false` | `Denied && (false \|\| true)` = `true` | **Forbidden** |

### 3.3 关键边界条件：匿名 + 规则有主体约束 → Unauthorized

```go
case required == authorization.Denied && (level != authentication.NotAuthenticated || !ruleHasSubject):
```

当 `level == NotAuthenticated && ruleHasSubject == true` 时：
- `level != authentication.NotAuthenticated` = `false`
- `!ruleHasSubject` = `false`
- 整个条件 = `false` → 不匹配此 case
- 进入 `default` → 返回 `AuthzResultUnauthorized`

**语义**：匿名用户命中一条带主体约束的 deny 规则时，不直接返回 403，而是返回"需要登录"。原因是：**匿名用户无法判断其身份是否满足主体约束**，登录后可能属于允许访问的组，也可能真正被拒绝。

### 3.4 `ruleHasSubject` 标记的真实来源

`HasSubjects` 不仅来自 `Subjects` 字段，还来自域名和资源中的用户/组占位符：

```go
// access_control_rule.go:28-30
if len(r.Subjects) != 0 {
    r.HasSubjects = true
}

// util.go:79-88
func ruleAddDomain(domainRules []string, rule *AccessControlRule) {
    for _, domainRule := range domainRules {
        subjects, r := NewAccessControlDomain(domainRule)
        rule.Domains = append(rule.Domains, r)
        if !rule.HasSubjects && subjects {  // {user}/*.example.com 或 {group}/*.example.com
            rule.HasSubjects = true
        }
    }
}
```

**三种触发 `HasSubjects = true` 的来源**：

| 来源 | 配置示例 | 说明 |
|---|---|---|
| Subjects 字段 | `subject: ["group:admins"]` | 显式配置的用户/组约束 |
| 域名占位符 | `domain: "{user}.example.com"` | 动态域名匹配用户名 |
| 域名/资源正则占位符 | `domain_regex: "^(?P<User>...)\.example\.com$"` | 正则中的命名捕获组 |

**影响**：一条规则可能没有显式 `subject` 配置，但因包含 `{user}` 域名占位符而被标记为 `HasSubjects = true`。这使得即使 `MatchesSubjects` 对匿名用户返回 `true`（因为没有 Subjects 字段约束），`isAuthzResult` 仍会将其作为"有主体约束"的规则处理，对匿名用户返回 Unauthorized 而非 Forbidden。

---

## 四、热重载机制对用户数据与访问控制策略的影响范围

### 4.1 热重载的覆盖范围

Authelia 的热重载机制**仅覆盖用户身份数据**，**不覆盖访问控制策略**：

| 数据类型 | 是否支持热重载 | 机制 |
|---|---|---|
| 用户数据库（File 后端） | ✅ 支持 | `FileWatcher` + `FileUserProvider.Reload()` |
| LDAP 后端用户数据 | ❌ 不适用 | 每次请求实时查询，无需重载 |
| 访问控制规则 | ❌ 不支持 | `Authorizer` 在启动时一次性编译，无 Reload 接口 |
| 配置文件 | ❌ 不支持 | 需要重启服务 |

### 4.2 File 后端热重载详细流程

```
fsnotify 检测到 users.yml 文件变更 (Write/Create)
    ↓
FileWatcher.Run()  ← internal/service/file_watcher.go:114
    ↓ 过滤无关文件事件
    ↓
FileUserProvider.Reload()  ← internal/authentication/file_user_provider.go:55
    ├─ mutex.Lock()  [Provider 级互斥锁]
    ├─ 冷却时间检查 (防止 fsnotify 重复触发)
    ↓
FileUserDatabase.Load()  ← internal/authentication/file_user_provider_database.go:71
    ├─ yml.Read(path)  [锁外: 读取 + 解析新文件]
    ├─ m.Lock()  [Database 级互斥锁]
    ├─ ReadToFileUserDatabase(m, m.Extra)
    │   ├─ 新建空 map: users := map[string]FileUserDatabaseUserDetails{}
    │   ├─ 遍历新数据填充 users
    │   └─ db.Users = users  [整体替换，旧数据被 GC]
    ├─ LoadAliases()  [重建邮箱/大小写别名索引]
    ├─ m.Unlock()
    ├─ 设置冷却时间
    └─ mutex.Unlock()
```

### 4.3 旧数据清除的原子性分析

**`ReadToFileUserDatabase` 的关键操作**（`file_user_provider_database.go:349-369`）：

```go
func (m *FileDatabaseModel) ReadToFileUserDatabase(db *FileUserDatabase, extra ...) (err error) {
    users := map[string]FileUserDatabaseUserDetails{}  // 1. 新建空 map

    for username, details := range m.Users {
        // 2. 验证和转换每个用户条目
        users[username] = *udm  // 3. 填充新数据
    }

    db.Users = users  // 4. 整体替换 → 旧数据清除
    return nil
}
```

**原子性保证**：
- 步骤 1-3 在 `m.Lock()` 保护下但操作的是**局部变量** `users`
- 步骤 4 是单次指针赋值 `db.Users = users`，Go 中 map 赋值是原子的
- 旧的 `db.Users` map 不再被引用，在 GC 时被回收

**并发安全的双重锁机制**：
- `FileUserProvider.mutex`：保护整个 Reload 过程的串行化
- `FileUserDatabase.sync.RWMutex`：保护数据库读写，`GetUserDetails` 使用 `RLock()` 读锁

### 4.4 热重载对策略评估的影响范围

#### 受影响的数据（热重载后立即生效）

| 数据项 | 影响范围 | 生效时机 |
|---|---|---|
| 用户组列表 | `Subject.Groups` → 组匹配结果 | 下次 `GetDetails` 调用时 |
| 用户密码 | 认证结果 | 下次 `CheckUserPassword` 调用时 |
| 用户禁用状态 | 用户是否存在 | 下次 `GetDetails` / `CheckUserPassword` 调用时 |
| 用户邮箱 | 会话刷新 | 下次 `handleSessionValidateRefresh` 时 |

#### 不受影响的数据（热重载无法改变）

| 数据项 | 原因 | 变更方式 |
|---|---|---|
| 访问控制规则 | `Authorizer.rules` 启动时一次性编译 | 重启服务 |
| 默认策略 | `Authorizer.defaultPolicy` 启动时设定 | 重启服务 |
| MFA 启用状态 | `Authorizer.mfa` 启动时计算 | 重启服务 |
| 域名/资源匹配规则 | 编译为 `AccessControlDomain`/`AccessControlResource` | 重启服务 |
| 主体匹配规则 | 编译为 `AccessControlSubjects` | 重启服务 |
| 网络匹配规则 | 编译为 `AccessControlNetworks` | 重启服务 |

#### 关键影响差异场景

**场景 1：用户组变更（热重载可生效）**

```
1. users.yml 中将 john 从 developers 组移除
2. 文件变更触发 FileWatcher → Reload → 数据库更新
3. Cookie 策略：下次 refresh_interval 到期时 GetDetails 返回新组列表
   → 会话 Groups 更新 → 下次请求使用新组进行策略评估
4. Header 策略：下次请求 GetDetails 立即返回新组列表
   → 策略评估立即使用新组
```

**场景 2：访问控制规则变更（热重载无法生效）**

```
1. config.yml 中新增一条访问控制规则
2. 配置文件变更不会触发任何重载机制
3. Authorizer.rules 仍然是启动时编译的旧规则
4. 必须重启 Authelia 服务才能生效
```

**场景 3：组名变更的不一致性风险**

```
1. config.yml 中规则引用 group:developers
2. users.yml 中将组名从 developers 改为 devs
3. 热重载更新了用户数据库，用户现在属于 devs 组
4. 但访问控制规则仍匹配 group:developers
5. 结果：用户失去 developers 组的访问权限，且 devs 组无对应规则
```

这种**用户数据与策略规则的变更不同步**是热重载机制的固有局限。

### 4.5 会话刷新与策略评估的交互

`internal/handlers/handler_authz_authn.go:498-555` 中的 `handleSessionValidateRefresh`：

```go
func handleSessionValidateRefresh(ctx AuthzContext, userSession *session.UserSession, 
    refresh schema.RefreshIntervalDuration) (modified, invalid bool) {
    if refresh.Never() || userSession.IsAnonymous() {
        return false, false  // 不刷新匿名会话或配置为 Never
    }

    if !refresh.Always() && userSession.RefreshTTL.After(ctx.GetClock().Now()) {
        return false, false  // TTL 未过期，跳过刷新
    }

    details, err := ctx.GetProviders().UserProvider.GetDetails(userSession.Username)
    // ...

    // 检测差异
    diffGroups = utils.IsStringSlicesDifferent(userSession.Groups, details.Groups)

    if !diffEmails && !diffGroups && !diffDisplayName {
        return modified, false  // 无变化
    }

    // 更新会话中的组信息
    userSession.Groups = details.Groups
    return true, false
}
```

**关键时序**：
1. 请求到达 → Cookie 策略获取会话 → 检查是否需要刷新
2. 如果 TTL 过期 → 调用 `GetDetails` 获取最新组信息 → 更新会话
3. 用**更新后的**组信息构造 `Subject` 进行策略评估
4. 因此，策略评估使用的组信息**最多滞后 `refresh_interval`**

---

## 五、RuleMatchResult 中 MatchSubjects 与 MatchSubjectsExact 的语义区分

### 5.1 两个匹配结果的差异

`internal/authorization/types.go:99-122`：

```go
type RuleMatchResult struct {
    MatchSubjects      bool  // MatchesSubjects 的结果
    MatchSubjectsExact bool  // MatchesSubjectExact 的结果
}

func (r RuleMatchResult) IsMatch() (match bool) {
    return r.MatchDomain && r.MatchResources && r.MatchQuery && 
           r.MatchMethods && r.MatchNetworks && r.MatchSubjectsExact
}

func (r RuleMatchResult) IsPotentialMatch() (match bool) {
    return r.MatchDomain && r.MatchResources && r.MatchQuery && 
           r.MatchMethods && r.MatchNetworks && r.MatchSubjects && !r.MatchSubjectsExact
}
```

**`IsMatch` 使用 `MatchSubjectsExact`**（精确匹配），而非 `MatchSubjects`（宽松匹配）。

**`IsPotentialMatch` 表示**：所有其他维度匹配，`MatchSubjects` 为 true 但 `MatchSubjectsExact` 为 false —— 这仅发生在**匿名用户 + 规则有主体约束**的场景。

```
匿名用户请求:
  MatchSubjects = true      (MatchesSubjects 对匿名用户返回 true)
  MatchSubjectsExact = false (MatchesSubjectExact 对匿名用户 + 有主体约束返回 false)
  → IsPotentialMatch = true → 该规则是"潜在匹配"

已认证用户请求:
  MatchSubjects = MatchSubjectsExact = true/false (两者一致)
  → IsPotentialMatch = false (不可能出现"潜在匹配")
```

这再次印证了 Authelia 的两阶段设计：`MatchSubjects` 用于第一阶段的宽松匹配，`MatchSubjectsExact` 用于第二阶段的精确判定。`IsPotentialMatch` 正是标识那些"匿名用户可能通过登录满足主体约束"的规则。

---

## 六、完整策略评估修正流程图

```
HTTP 请求
    ↓
handler_authz.go: Handler()
    ├─ 构造 Object (URL, Method, Domain)
    ├─ authn = authz.authn()  → 认证策略获取用户信息
    │   ├─ Cookie策略: 会话缓存 + handleSessionValidateRefresh
    │   │   → UserDetails.Groups 来自会话(可能已刷新)
    │   └─ Header策略: 每次请求调用 GetDetails
    │       → UserDetails.Groups 来自后端实时查询
    ├─ 构造 Subject{Username, Groups, ClientID, IP}
    ↓
authorizer.go: GetRequiredLevel(subject, object)
    ├─ 按顺序遍历 rules
    │   ↓
    │   IsMatch(subject, object)
    │   ├─ MatchesDomains()  → 支持 {user}/{group} 动态匹配
    │   │   → {group}.example.com 需要检查 subject.Groups
    │   ├─ MatchesResources() → 支持 (?P<Group>...) 正则
    │   ├─ MatchesQuery()
    │   ├─ MatchesMethods()
    │   ├─ MatchesNetworks()
    │   └─ MatchesSubjects()
    │       ├─ 匿名用户 → return true [第一阶段: 宽松匹配]
    │       └─ 已认证用户 → MatchesSubjectExact()
    │           ├─ 无 Subjects 约束 → true
    │           ├─ 有 Subjects + 匿名 → false
    │           └─ 有 Subjects + 已认证 → 遍历匹配
    │               ├─ 组间 OR: []AccessControlSubjects 任意一个匹配
    │               └─ 组内 AND: AccessControlSubjects.Subjects 全部匹配
    │                   ├─ AccessControlUser.IsMatch() → username 精确匹配
    │                   ├─ AccessControlGroup.IsMatch() → Groups 包含匹配
    │                   └─ AccessControlClient.IsMatch() → ClientID 精确匹配
    ├─ 命中 → return (rule.HasSubjects, rule.Policy)
    │   HasSubjects 来源:
    │   ├─ len(Subjects) > 0 (显式 subject 配置)
    │   ├─ 域名含 {user}/{group} 占位符
    │   └─ 域名/资源正则含 (?P<User>/...(?P<Group>...)
    └─ 无命中 → return (false, defaultPolicy)
    ↓
isAuthzResult(authn.Level, required, ruleHasSubject)
    ├─ Bypass → Authorized (200)
    ├─ Denied + (已认证 || 无主体约束) → Forbidden (403)
    ├─ Denied + 匿名 + 有主体约束 → Unauthorized (302/401)
    │   [设计意图: 匿名用户登录后可能满足主体约束]
    ├─ OneFactor/TwoFactor + 级别足够 → Authorized (200)
    └─ OneFactor/TwoFactor + 级别不足 → Unauthorized (302/401)
```

---

## 七、关键修正总结

### 7.1 上次分析中的错误修正

| 项 | 上次描述 | 修正后 |
|---|---|---|
| MatchesSubjects 匿名逻辑 | "匿名用户自动通过主体检查（规则不包含主体约束时）" | **匿名用户无条件通过**，无论规则是否包含主体约束；这是两阶段设计的第一阶段宽松匹配 |
| HasSubjects 来源 | 仅来自 Subjects 字段 | 还来自域名的 `{user}`/`{group}` 占位符和正则命名组 |
| isAuthzResult Denied 逻辑 | "用户已认证或规则无主体约束 → 直接禁止" | 完整条件是 `level != NotAuthenticated || !ruleHasSubject`，匿名+有主体约束时返回 Unauthorized 是**有意设计** |
| 热重载影响范围 | "旧策略的清除机制" | **访问控制策略（Authorizer）根本不支持热重载**，热重载仅影响用户身份数据 |
| 组信息时效性 | 未区分 | Cookie 策略有 `refresh_interval` 延迟，Header 策略实时获取 |

### 7.2 设计要点总结

1. **两阶段判定**：第一阶段 `MatchesSubjects` 对匿名用户宽松匹配，第二阶段 `isAuthzResult` 结合 `ruleHasSubject` 精确判定拒绝语义
2. **HasSubjects 的复合语义**：标识规则是否依赖用户身份，来源包括显式 subject、域名/资源占位符
3. **热重载范围有限**：仅覆盖 File 后端用户数据，不覆盖访问控制规则、配置变更需要重启
4. **组信息时效性差异**：Cookie 策略受 `refresh_interval` 约束，Header 策略实时获取
5. **数据变更不一致风险**：用户组名变更后，热重载更新了用户数据但访问控制规则仍引用旧组名
6. **并发安全双重锁**：Provider 级 `mutex` + Database 级 `sync.RWMutex`，通过整体替换 map 实现原子数据切换
