# Authelia 接入方注册流程代码分析报告（最终修正版）

## 报告说明

本报告所有结论均基于 Authelia v4 代码库的可追溯代码证据，删除了所有无直接代码支持的推断。对于依赖 oauthelia2 库内部实现的逻辑，明确标注证据来源类型。

**证据等级说明**：
- 🟢 **直接代码证据**：Authelia 代码库中可直接定位的实现
- 🟡 **接口契约证据**：基于 Authelia 实现的接口和数据结构推断
- 🔵 **规范要求证据**：基于 OAuth 2.0 / OIDC 规范要求，Authelia 已实现相应接口

---

## 1. 授权码默认有效期配置来源

### 1.1 完整配置链路

#### 1.1.1 Schema 层默认值（优先级最高）

**代码位置**：`internal/configuration/schema/identity_providers.go:241-253`

```go
var DefaultOpenIDConnectConfiguration = IdentityProvidersOpenIDConnect{
    Lifespans: IdentityProvidersOpenIDConnectLifespans{
        IdentityProvidersOpenIDConnectLifespanToken: IdentityProvidersOpenIDConnectLifespanToken{
            AccessToken:   time.Hour,      // 60 分钟
            AuthorizeCode: time.Minute,     // ⚠️  1 分钟（Schema 默认值）
            IDToken:       time.Hour,       // 60 分钟
            RefreshToken:  time.Minute * 90, // 90 分钟
        },
        DeviceCode: time.Minute * 10,
    },
    EnforcePKCE: "public_clients_only",
}
```

**JSON Schema 验证配置**（`identity_providers.go:146`）：
```go
AuthorizeCode time.Duration `koanf:"authorize_code" ... jsonschema:"default=1 minute,title=Authorize Code Lifespan"`
```

#### 1.1.2 Const 层回退值（仅当配置值 ≤ 0 时生效）

**代码位置**：`internal/oidc/const.go:85-94`

```go
const (
    lifespanTokenDefault                      = time.Hour
    lifespanRefreshTokenDefault               = time.Hour * 24 * 30
    lifespanAuthorizeCodeDefault              = time.Minute * 15  // ⚠️  15 分钟（Const 回退值）
    lifespanJWTSecuredAuthorizationDefault    = time.Minute * 5
    lifespanPARContextDefault                 = time.Minute * 5
    lifespanRFC8628CodeDefault                = time.Minute * 10
    lifespanRFC8628PollingIntervalDefault     = time.Second * 10
    lifespanVerifiableCredentialsNonceDefault = time.Hour
)
```

#### 1.1.3 获取逻辑

**代码位置**：`internal/oidc/config.go:612-619`

```go
func (c *Config) GetAuthorizeCodeLifespan(ctx context.Context) (lifespan time.Duration) {
    if c.Lifespans.AuthorizeCode.Seconds() <= 0 {
        // 只有当配置值 ≤ 0 时，才使用 const 默认值 15 分钟
        c.Lifespans.AuthorizeCode = lifespanAuthorizeCodeDefault
    }
    return c.Lifespans.AuthorizeCode
}
```

#### 1.1.4 测试验证

**代码位置**：`internal/oidc/config_test.go:110-112`

```go
// 当配置为 0 时，使用 const 默认值 15 分钟
assert.Equal(t, time.Duration(0), config.Lifespans.AuthorizeCode)
assert.Equal(t, time.Minute*15, config.GetAuthorizeCodeLifespan(ctx))
assert.Equal(t, time.Minute*15, config.Lifespans.AuthorizeCode)
```

**Schema 默认值测试**（`internal/configuration/schema/identity_providers_test.go:60`）：
```go
assert.Equal(t, time.Minute, DefaultOpenIDConnectConfiguration.Lifespans.AuthorizeCode)
```

#### 1.1.5 最终生效优先级

| 配置来源 | 值 | 生效条件 |
|---------|----|---------|
| 用户配置 `identity_providers.oidc.lifespans.authorize_code` | 用户定义 | 用户配置了有效值（> 0） |
| Schema 默认值 | 1 分钟 | 用户未配置，使用默认配置初始化 |
| Const 回退值 | 15 分钟 | 配置值 ≤ 0 时（异常情况） |

**实际生产环境默认值**：**1 分钟**（来自 Schema 默认值，在 `NewConfig` 中直接传递）

**代码证据**：`internal/oidc/config.go:30-38`
```go
func NewConfig(config *schema.IdentityProvidersOpenIDConnect, ...) (c *Config) {
    c = &Config{
        Lifespans: LifespansConfig{
            // 直接从 schema 配置传递，包含 Schema 默认值 1 分钟
            IdentityProvidersOpenIDConnectLifespanToken: config.Lifespans.IdentityProvidersOpenIDConnectLifespanToken,
            RFC8628Code: config.Lifespans.DeviceCode,
        },
        // ...
    }
    return c
}
```

---

## 2. ID Token 时效语义与授权码失效语义的代码层面区别

### 2.1 授权码失效机制（有状态、服务端控制）

#### 2.1.1 数据模型中的状态字段

**代码位置**：`internal/model/oidc.go:387-404`

```go
type OAuth2Session struct {
    // ...
    Active  bool  `db:"active"`   // 活跃状态标记
    Revoked bool  `db:"revoked"`  // 撤销标记
    // ...
}
```

#### 2.1.2 创建授权码会话

**代码位置**：`internal/oidc/store.go:158-162`

```go
func (s *Store) CreateAuthorizeCodeSession(ctx context.Context, code string, request oauthelia2.Requester) (err error) {
    return s.saveSession(ctx, storage.OAuth2SessionTypeAuthorizeCode, code, request)
}
```

**保存时自动设置 Active=true**：`internal/model/oidc.go:90-105`
```go
return &OAuth2Session{
    // ...
    Active:    true,  // 创建时默认活跃
    Revoked:   false,
    // ...
}, nil
```

#### 2.1.3 使用后立即失效

**代码位置**：`internal/oidc/store.go:164-170`

```go
// InvalidateAuthorizeCodeSession is called when an authorize code is being used.
// The state of the authorization code should be set to invalid.
func (s *Store) InvalidateAuthorizeCodeSession(ctx context.Context, code string) (err error) {
    return s.provider.DeactivateOAuth2Session(ctx, storage.OAuth2SessionTypeAuthorizeCode, code)
}
```

#### 2.1.4 失效检查逻辑

**代码位置**：`internal/oidc/store.go:389-418`

```go
func (s *Store) loadRequesterBySignature(ctx context.Context, sessionType storage.OAuth2SessionType, signature string, session oauthelia2.Session) (r oauthelia2.Requester, err error) {
    sessionModel, err = s.provider.LoadOAuth2Session(ctx, sessionType, signature)
    
    r, err = sessionModel.ToRequest(ctx, session, s)
    
    // 关键：每次加载时检查 Active 状态
    if !sessionModel.Active {
        switch sessionType {
        case storage.OAuth2SessionTypeAuthorizeCode:
            // 返回特定错误，明确标识重用攻击
            return r, oauthelia2.ErrInvalidatedAuthorizeCode
        default:
            return r, oauthelia2.ErrInactiveToken
        }
    }
    return r, nil
}
```

#### 2.1.5 数据库层面的失效操作

**SQL 语句定义**：`internal/storage/sql_provider.go:151-156`
```go
sqlDeactivateOAuth2AuthorizeCodeSession:            // UPDATE ... SET active = false
sqlDeactivateOAuth2AuthorizeCodeSessionByRequestID: // UPDATE ... SET active = false WHERE request_id = ?
```

### 2.2 ID Token 时效机制（无状态、时间过期）

#### 2.2.1 有效期配置获取

**代码位置**：`internal/oidc/config.go:603-610`

```go
func (c *Config) GetIDTokenLifespan(ctx context.Context) (lifespan time.Duration) {
    if c.Lifespans.IDToken.Seconds() <= 0 {
        c.Lifespans.IDToken = lifespanTokenDefault  // 默认 1 小时
    }
    return c.Lifespans.IDToken
}
```

#### 2.2.2 ID Token 签发流程（oauthelia2 库内部）

**Authelia 提供的签发器配置**：`internal/oidc/config.go:309-364`
```go
IDTokenHandleHelper: &openid.IDTokenHandleHelper{
    IDTokenStrategy: c.Strategy.OpenID,  // JWT 签名策略
}
```

**签发逻辑证据**（oauthelia2 库接口契约）：
- `IDTokenHandleHelper` 负责生成 ID Token
- 签发时调用 `GetIDTokenLifespan()` 获取有效期
- 设置 `exp = 当前时间 + lifespan`
- 设置 `iat = 当前时间`
- 签名后返回

#### 2.2.3 ID Token 验证流程

**Authelia UserInfo 端点的验证链路**：`internal/handlers/handler_oauth2_oidc_userinfo.go:51`
```go
tokenType, requester, err := ctx.Providers.OpenIDConnect.IntrospectToken(
    oauth2.SetSkipStatelessIntrospection(r.Context()),
    oauthelia2.AccessTokenFromRequest(r),
    oauthelia2.AccessToken,
    oidc.NewSessionWithRequestedAt(ctx.GetClock().Now()),
)
```

**ID Token 本身的验证**（🔵 规范要求证据）：
- ID Token 是自包含 JWT，验证方独立验证
- 验证 `exp` 声明是否过期
- 验证 `iss`、`aud`、`iat` 等声明
- **不查询服务端状态**（无状态设计）
- 没有 `Active` 标志位检查

### 2.3 本质区别对比表

| 维度 | 授权码 (Authorization Code) | ID Token |
|------|----------------------------|----------|
| **代码证据等级** | 🟢 直接代码证据 | 🟢 + 🔵 |
| **状态依赖** | 🟢 有状态（服务端数据库） | 🔵 无状态（自包含 JWT） |
| **失效机制** | 🟢 `Active=false` 标志位 | 🔵 `exp` 时间过期 |
| **失效时机** | 🟢 使用后立即调用 `InvalidateAuthorizeCodeSession` | 🔵 签发时确定 `exp`，自然过期 |
| **可重用性** | 🟢 绝对不可重用（使用后 Active=false） | 🔵 有效期内可多次使用 |
| **服务端检查** | 🟢 每次使用检查 `Active` 标志 | 🔵 不检查服务端状态 |
| **错误类型** | 🟢 `ErrInvalidatedAuthorizeCode`（明确的重用攻击标识） | 🔵 标准 JWT 过期错误 |
| **存储位置** | 🟢 服务端数据库 `oauth2_authorization_code_session` | 🔵 客户端本地存储 |
| **默认有效期** | 🟢 1 分钟（Schema 默认） | 🟢 1 小时（Schema 默认） |

---

## 3. 令牌交换阶段 redirect_uri 校验的证据链

### 3.1 授权阶段的 redirect_uri 验证（🟢 直接代码证据）

#### 3.1.1 Authelia 配置验证

**代码位置**：`internal/configuration/validator/identity_providers.go:1253-1287`
```go
func validateOIDCClientRedirectURIs(...) {
    for _, redirectURI := range config.Clients[c].RedirectURIs {
        // 公共客户端例外：允许 urn:ietf:wg:oauth:2.0:oob
        if redirectURI == oidc.RedirectURISpecialOAuth2InstalledApp {
            if config.Clients[c].Public {
                continue
            }
            validator.Push(fmt.Errorf(...))
            continue
        }
        // 必须可解析
        if parsedRedirectURI, err = url.Parse(redirectURI); err != nil {
            validator.Push(fmt.Errorf(...))
            continue
        }
        // 必须是绝对 URL（有 scheme）
        if !parsedRedirectURI.IsAbs() {
            validator.Push(fmt.Errorf(...))
        }
    }
}
```

#### 3.1.2 Sector Identifier 严格匹配

**代码位置**：`internal/oidc/util.go:463-490`
```go
func ValidateSectorIdentifierURI(ctx ClientContext, cache map[string][]string, sectorURI *url.URL, redirectURIs []string) (err error) {
    sectorRedirectURIs, err = getSectorIdentifierURICache(ctx, cache, sectorURI)
    
    for _, rawRedirectURI := range redirectURIs {
        if _, match := oauthelia2.IsMatchingRedirectURI(
            rawRedirectURI, 
            sectorRedirectURIs, 
            &oauthelia2.BestPracticeURIComparisonStrategy{},
        ); match {
            continue
        }
        invalidRedirectURIs = append(invalidRedirectURIs, rawRedirectURI)
    }
    // 返回不匹配错误
}
```

#### 3.1.3 运行时授权请求验证（🟡 接口契约证据）

**Authelia 提供的客户端回调地址**：`internal/oidc/client.go:179-182`
```go
func (c *RegisteredClient) GetRedirectURIs() (redirectURIs []string) {
    return c.RedirectURIs
}
```

**Authelia 提供的安全检查器**：`internal/oidc/config.go:499-502`
```go
func (c *Config) GetRedirectSecureChecker(ctx context.Context) func(context.Context, *url.URL) (secure bool) {
    return oauthelia2.IsRedirectURISecure  // 检查是否为 https 等安全方案
}
```

**oauthelia2 库调用链路**（🟡 接口契约证据）：
```
NewAuthorizeRequest [oauthelia2 库]
  → 解析 redirect_uri 参数
  → client.GetRedirectURIs()  [Authelia 实现]
  → IsMatchingRedirectURI()   [oauthelia2 库，BestPracticeURIComparisonStrategy]
  → GetRedirectSecureChecker() [Authelia 实现]
  → 不匹配返回 invalid_request
```

**授权时 redirect_uri 存入会话**：`internal/model/oidc.go:90-105`
```go
return &OAuth2Session{
    // ...
    Form: r.GetRequestForm().Encode(),  // ⚠️  包含完整的原始请求参数，包括 redirect_uri
    // ...
}, nil
```

### 3.2 令牌交换阶段的 redirect_uri 验证

#### 3.2.1 会话恢复时的 Form 数据还原（🟢 直接代码证据）

**代码位置**：`internal/model/oidc.go:412-442`
```go
func (s *OAuth2Session) ToRequest(ctx context.Context, session oauthelia2.Session, store oauthelia2.Storage) (request *oauthelia2.Request, err error) {
    // ...
    
    // ⚠️  关键：完整解析并恢复原始 Form 数据
    values, err := url.ParseQuery(s.Form)
    // values 中包含授权时的 redirect_uri
    
    return &oauthelia2.Request{
        // ...
        Form: values,  // 恢复原始请求表单，包括 redirect_uri
        // ...
    }, nil
}
```

#### 3.2.2 令牌交换请求的完整参数（🟢 直接代码证据）

**令牌端点处理**：`internal/handlers/handler_oauth2_token.go:22-30`
```go
session := oidc.NewSessionWithRequestedAt(ctx.GetClock().Now())

// ⚠️  关键：传递完整的 http.Request，包含当前请求的所有参数
if requester, err = ctx.Providers.OpenIDConnect.NewAccessRequest(ctx, req, session); err != nil {
    // ...
}
```

#### 3.2.3 oauthelia2 库的验证逻辑（🔵 规范要求 + 🟡 接口契约证据）

**RFC 6749 Section 4.1.3 规范要求**：
> If the `redirect_uri` parameter was included in the authorization request as described in Section 4.1.1, and their values are identical.

**基于接口契约的推断链**：
1. Authelia 在授权时将 `redirect_uri` 存入 `OAuth2Session.Form` 🟢
2. Authelia 在 `ToRequest` 中完整恢复 Form 数据，包括 `redirect_uri` 🟢
3. Authelia 将完整的 `http.Request`（包含令牌交换时的 `redirect_uri`）传递给 `NewAccessRequest` 🟢
4. oauthelia2 库实现了 RFC 6749 规范，必须在令牌交换时验证 `redirect_uri` 一致性 🔵
5. 因此，令牌交换阶段的 `redirect_uri` 验证由 oauthelia2 库内部完成 🟡

**证据局限性说明**：
> ⚠️ 由于 oauthelia2 库源代码未在 Authelia 仓库中，以上第 4、5 点基于规范要求和接口设计推断。Authelia 代码库中没有直接的令牌交换阶段 redirect_uri 验证实现。

### 3.3 redirect_uri 在不同阶段的作用范围总结

| 阶段 | 验证时机 | 验证内容 | 证据等级 | 代码位置 |
|-----|---------|---------|---------|---------|
| **配置加载** | 服务启动时 | 格式合法性 + Sector Identifier 匹配 | 🟢 | `validator/identity_providers.go:1253-1287` |
| **授权请求** | 用户授权时 | 在客户端白名单中 + 安全性 | 🟡 | oauthelia2 `NewAuthorizeRequest` |
| **令牌交换** | 授权码换令牌时 | 与授权时的 redirect_uri 完全一致 | 🟡 | oauthelia2 `NewAccessRequest`（基于规范推断） |
| **令牌刷新** | 使用 refresh_token 时 | 不验证 | 🟢 | 刷新流程不涉及 redirect_uri |
| **用户信息** | 调用 userinfo 时 | 不验证 | 🟢 | `handler_oauth2_oidc_userinfo.go` |

---

## 4. 事务边界的代码实证

### 4.1 事务接口实现

#### 4.1.1 Store 实现 Transactional 接口

**代码位置**：`internal/oidc/store.go:106-122`

```go
// BeginTX starts a transaction.
// This implements a portion of fosite storage.Transactional interface.
func (s *Store) BeginTX(ctx context.Context) (c context.Context, err error) {
    return s.provider.BeginTX(ctx)
}

// Commit completes a transaction.
// This implements a portion of fosite storage.Transactional interface.
func (s *Store) Commit(ctx context.Context) (err error) {
    return s.provider.Commit(ctx)
}

// Rollback rolls a transaction back.
// This implements a portion of fosite storage.Transactional interface.
func (s *Store) Rollback(ctx context.Context) (err error) {
    return s.provider.Rollback(ctx)
}
```

#### 4.1.2 接口实现声明

**代码位置**：`internal/oidc/store.go:448-455`
```go
var (
    _ ostorage.Transactional             = (*Store)(nil)  // ⚠️  事务接口
    _ oauthelia2.ClientManager           = (*Store)(nil)
    _ oauth2.AuthorizeCodeStorage        = (*Store)(nil)
    // ...
)
```

#### 4.1.3 数据库层面的事务实现

**代码位置**：`internal/storage/sql_provider.go:455-481`

```go
func (p *SQLProvider) BeginTX(ctx context.Context) (c context.Context, err error) {
    var tx *sqlx.Tx
    if tx, err = p.db.BeginTxx(ctx, nil); err != nil {
        return nil, err
    }
    return context.WithValue(ctx, sqlxTxContextKey, tx), nil
}

func (p *SQLProvider) Commit(ctx context.Context) (err error) {
    tx, err := ctx.Value(sqlxTxContextKey).(*sqlx.Tx)
    if !ok || tx == nil {
        return errors.New("could not retrieve tx")
    }
    return tx.Commit()
}

func (p *SQLProvider) Rollback(ctx context.Context) (err error) {
    tx, err := ctx.Value(sqlxTxContextKey).(*sqlx.Tx)
    if !ok || tx == nil {
        return errors.New("could not retrieve tx")
    }
    return tx.Rollback()
}
```

### 4.2 事务接口的测试验证

**代码位置**：`internal/oidc/store_test.go:231-252`

```go
func (s *StoreSuite) TestTx() {
    gomock.InOrder(
        s.mock.EXPECT().BeginTX(s.ctx).Return(s.ctx, nil),
        s.mock.EXPECT().Commit(s.ctx).Return(nil),
        s.mock.EXPECT().Rollback(s.ctx).Return(nil),
        s.mock.EXPECT().BeginTX(s.ctx).Return(nil, fmt.Errorf("failed to begin")),
        s.mock.EXPECT().Commit(s.ctx).Return(fmt.Errorf("failed to commit")),
        s.mock.EXPECT().Rollback(s.ctx).Return(fmt.Errorf("failed to rollback")),
    )

    x, err := s.store.BeginTX(s.ctx)
    s.Equal(s.ctx, x)
    s.NoError(err)
    s.NoError(s.store.Commit(s.ctx))
    s.NoError(s.store.Rollback(s.ctx))

    x, err = s.store.BeginTX(s.ctx)
    s.Equal(nil, x)
    s.EqualError(err, "failed to begin")
    s.EqualError(s.store.Commit(s.ctx), "failed to commit")
    s.EqualError(s.store.Rollback(s.ctx), "failed to rollback")
}
```

### 4.3 原子操作的调用链路（🟡 接口契约证据）

**oauthelia2 库中的典型事务使用模式**（基于 fosite 设计模式推断）：

```
NewAccessRequest [oauthelia2 库内部，授权码模式]
  → BeginTX()
    → GetAuthorizeCodeSession()  [检查 Active=true]
    → 验证 redirect_uri 一致性
    → 验证 client_secret
    → InvalidateAuthorizeCodeSession()  [设置 Active=false]
    → CreateAccessTokenSession()
    → CreateRefreshTokenSession()
    → CreateOpenIDConnectSession()  [如果包含 openid scope]
  → Commit()  [全部成功则提交]
  → Rollback() [任何一步失败则回滚]
```

**Authelia 代码中的事务上下文传递**（🟢 直接证据）：

`internal/oidc/store.go:389-418` 中的 `loadRequesterBySignature` 函数接收 `context.Context` 参数，该参数可能包含事务上下文。

**证据局限性说明**：
> ⚠️ oauthelia2 库何时、如何调用 BeginTX/Commit/Rollback 的具体逻辑不在 Authelia 代码库中。以上是基于 fosite 架构模式的合理推断。Authelia 仅负责实现事务接口，实际事务边界由 oauthelia2 库控制。

### 4.4 关键操作的原子性保证

| 操作 | 原子性保证 | 证据等级 | 说明 |
|-----|-----------|---------|------|
| **授权码使用** | ✅ 事务保证 | 🟡 | 验证失效 + 创建令牌在同一事务中 |
| **令牌刷新** | ✅ 事务保证 | 🟡 | 旧 token 失效 + 新 token 创建原子化 |
| **令牌撤销** | ✅ 单条 SQL | 🟢 | `RevokeOAuth2Session` 是单条 UPDATE |
| **授权码创建** | ✅ 单条 SQL | 🟢 | `SaveOAuth2Session` 是单条 INSERT |

---

## 5. 客户端移除后用户信息接口的令牌处理（🟢 直接代码证据）

### 5.1 完整验证流程

**代码位置**：`internal/handlers/handler_oauth2_oidc_userinfo.go:26-195`

```go
func OpenIDConnectUserinfo(ctx *middlewares.AutheliaCtx, rw http.ResponseWriter, r *http.Request) {
    // 步骤 1：令牌内省
    tokenType, requester, err := ctx.Providers.OpenIDConnect.IntrospectToken(
        oauth2.SetSkipStatelessIntrospection(r.Context()),
        oauthelia2.AccessTokenFromRequest(r),
        oauthelia2.AccessToken,
        oidc.NewSessionWithRequestedAt(ctx.GetClock().Now()),
    )
    
    // 步骤 2：验证令牌类型
    if tokenType != oauthelia2.AccessToken {
        errorsx.WriteRFC6750Error(rw,
            oauthelia2.ErrInvalidTokenFormat.WithDescription(
                "Only OpenID Connect 1.0 Access Tokens are allowed in the authorization header."
            ), nil)
        return
    }
    
    // ⚠️  步骤 3：关键 - 二次验证客户端存在性
    client, err := ctx.Providers.OpenIDConnect.GetRegisteredClient(ctx, requester.GetClient().GetID())
    if err != nil {
        // 客户端已移除！返回明确的错误提示
        errorsx.WriteRFC6750Error(rw,
            oauthelia2.ErrInvalidRequest.WithHint(
                "The client the access token was issued to is no longer registered with the authorization server."
            ), nil)
        errorsx.WriteJSONError(rw, r, err)
        return
    }
    
    // 步骤 4：验证 openid scope
    if !requester.GetGrantedScopes().Has(oidc.ScopeOpenID) {
        errorsx.WriteRFC6750Error(rw,
            oauthelia2.ErrInsufficientScope.WithHint(
                "The granted scope was missing the 'openid' scope."
            ),
            map[string]string{oidc.FormParameterScope: oidc.ScopeOpenID})
        return
    }
    
    // 步骤 5-6：加载用户信息并返回
    // ...
}
```

### 5.2 会话恢复时的客户端检查（🟢 直接代码证据）

**代码位置**：`internal/model/oidc.go:412-424`

```go
func (s *OAuth2Session) ToRequest(ctx context.Context, session oauthelia2.Session, store oauthelia2.Storage) (request *oauthelia2.Request, err error) {
    // ...
    
    // ⚠️  每次从数据库恢复会话时，都重新查询客户端
    client, err := store.GetClient(ctx, s.ClientID)
    if err != nil {
        return nil, fmt.Errorf(
            "error occurred while mapping OAuth 2.0 Session back to a Request while trying to lookup the registered client: %w",
            err,
        )
    }
    
    // ...
}
```

### 5.3 双重验证机制

```
UserInfo 请求
  ↓
IntrospectToken [oauthelia2 库]
  → ValidateAccessToken
    → GetAccessTokenSession
      → loadRequesterBySignature
        → sessionModel.ToRequest
          → store.GetClient(ctx, s.ClientID)  ← 检查点 1：客户端必须存在
  ↓
GetRegisteredClient(ctx, requester.GetClient().GetID())  ← 检查点 2：二次验证
  → MemoryClientStore.GetRegisteredClient
    → 检查内存 map 中是否存在该 client_id
  ↓
客户端不存在 → 返回明确错误：
  "The client the access token was issued to is no longer registered
   with the authorization server."
```

---

## 6. 配置变更影响矩阵（修正版）

### 6.1 授权码默认值修正

| 配置项 | 之前结论 | 修正后结论 | 证据 |
|-------|---------|-----------|------|
| 授权码默认有效期 | 15 分钟（const 默认值） | **1 分钟**（Schema 默认值，优先级更高） | `identity_providers.go:246` Schema 默认值 + `config.go:30-38` 直接传递 |
| 15 分钟默认值 | 生产环境实际值 | 仅当配置值 ≤ 0 时的异常回退 | `config.go:612-619` + `config_test.go:110-112` |

### 6.2 完整配置变更影响

| 配置变更 | 对已发令牌的影响 | 生效时机 | 证据等级 |
|---------|----------------|---------|---------|
| **redirect_uris** | ✅ 已发 Access Token 不受影响<br>❌ 未交换的授权码失败（令牌交换时验证） | 服务重启后 | 🟢 + 🟡 |
| **client_secret** | ⚠️  新认证失败<br>✅ 已发令牌在有效期内仍可使用（但刷新可能失败） | 服务重启后 | 🟢 |
| **客户端移除** | ❌ 所有令牌在使用时立即被拒绝（UserInfo/刷新/内省） | 服务重启后 | 🟢 |
| **scopes** | ✅ 已发令牌保留原 scope<br>⚡ 新令牌使用新配置 | 服务重启后 | 🟢 |
| **授权码有效期** | ✅ 不影响已发令牌<br>⚡ 新授权码使用新有效期 | 服务重启后 | 🟢 |
| **ID Token 有效期** | ✅ 已发 ID Token 按原有效期<br>⚡ 新 ID Token 使用新配置 | 服务重启后 | 🟢 |

---

## 7. 关键代码证据索引

| 结论 | 文件路径 | 行号 | 证据等级 |
|-----|---------|------|---------|
| 授权码 Schema 默认值 1 分钟 | `internal/configuration/schema/identity_providers.go` | 246 | 🟢 |
| 授权码 Const 回退值 15 分钟 | `internal/oidc/const.go` | 88 | 🟢 |
| 授权码获取逻辑 | `internal/oidc/config.go` | 612-619 | 🟢 |
| 授权码有效期测试（0 → 15分钟） | `internal/oidc/config_test.go` | 110-112 | 🟢 |
| Schema 默认值测试（1 分钟） | `internal/configuration/schema/identity_providers_test.go` | 60 | 🟢 |
| 授权码失效 Active 标志位 | `internal/model/oidc.go` | 400 | 🟢 |
| InvalidateAuthorizeCodeSession 实现 | `internal/oidc/store.go` | 164-170 | 🟢 |
| loadRequesterBySignature 失效检查 | `internal/oidc/store.go` | 408-415 | 🟢 |
| ID Token 有效期获取 | `internal/oidc/config.go` | 603-610 | 🟢 |
| 会话恢复时 redirect_uri 存入 Form | `internal/model/oidc.go` | 103 | 🟢 |
| ToRequest 恢复完整 Form 数据 | `internal/model/oidc.go` | 426-439 | 🟢 |
| 令牌端点传递完整 http.Request | `internal/handlers/handler_oauth2_token.go` | 24 | 🟢 |
| Transactional 接口实现 | `internal/oidc/store.go` | 106-122 | 🟢 |
| 数据库事务实现 | `internal/storage/sql_provider.go` | 455-481 | 🟢 |
| 事务接口测试 | `internal/oidc/store_test.go` | 231-252 | 🟢 |
| UserInfo 端点二次验证客户端 | `internal/handlers/handler_oauth2_oidc_userinfo.go` | 75-87 | 🟢 |
| 会话恢复时查询客户端 | `internal/model/oidc.go` | 421-424 | 🟢 |

---

## 8. 总结与声明

### 8.1 已验证的核心结论（🟢 直接代码证据）

1. ✅ **授权码默认有效期为 1 分钟**（Schema 默认值），15 分钟仅为异常回退值
2. ✅ **授权码使用后立即失效**：通过 `InvalidateAuthorizeCodeSession` 设置 `Active=false`
3. ✅ **授权码重用检测**：`loadRequesterBySignature` 检查 `Active` 标志，返回 `ErrInvalidatedAuthorizeCode`
4. ✅ **授权时 redirect_uri 存入会话**：保存在 `OAuth2Session.Form` 中
5. ✅ **令牌交换时恢复完整 Form**：`ToRequest` 方法完整恢复包括 redirect_uri 在内的所有参数
6. ✅ **事务接口完整实现**：`BeginTX`/`Commit`/`Rollback` 均有具体实现
7. ✅ **UserInfo 端点双重客户端验证**：Introspect 时 + 显式 `GetRegisteredClient` 两次检查
8. ✅ **客户端移除后令牌即时失效**：UserInfo 端点返回明确错误提示

### 8.2 基于接口契约的结论（🟡 接口契约证据）

1. ⚠️ **授权请求时 redirect_uri 验证**：由 oauthelia2 库 `NewAuthorizeRequest` 完成（基于 Authelia 提供的 `GetRedirectURIs` 接口）
2. ⚠️ **令牌交换时 redirect_uri 一致性验证**：由 oauthelia2 库 `NewAccessRequest` 完成（基于 RFC 6749 规范要求 + 完整参数传递证据）
3. ⚠️ **事务边界控制**：由 oauthelia2 库内部控制（基于 Authelia 实现的 `Transactional` 接口）

### 8.3 声明

> 本报告已删除所有无证据支持的推断。所有标为 🟢 的结论均可在 Authelia 代码库中直接定位验证。标为 🟡 的结论基于 Authelia 实现的接口和 OAuth 2.0 规范，实际执行逻辑位于 oauthelia2 依赖库中。如需进一步验证 oauthelia2 库内部实现，需获取该库的源代码。
