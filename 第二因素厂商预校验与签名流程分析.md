# 第二因素厂商预校验与正式签名流程分析

## 1. 概述

本文档分析 Authelia 中第二因素认证（以 Duo 为例）的**预校验（PreAuth）**与**正式签名（Auth）**两次远端调用之间的状态衔接机制，包括失败分支处理、会话推进逻辑，以及上层策略评估与下层 HTTP 客户端之间的协议层切面。

---

## 2. 架构层次与切面划分

### 2.1 层次结构

```
┌─────────────────────────────────────────────────────────┐
│  上层策略评估层 (handlers)                              │
│  ┌───────────────────────────────────────────────────┐  │
│  │  HandleDuoAuthentication                          │  │
│  │  ├─ HandleInitialDeviceSelection / HandlePreferred│  │
│  │  └─ PerformDuoAuthenticationIfValid               │  │
│  └───────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────┤
│  协议层切面 (duo.Provider 接口)                          │
│  ┌─────────────────┐  ┌────────────────────────────┐  │
│  │  PreAuthCall    │  │  AuthCall                  │  │
│  └─────────────────┘  └────────────────────────────┘  │
├─────────────────────────────────────────────────────────┤
│  下层 HTTP 客户端 (duoapi.SignedCall)                   │
│  签名、加密、网络传输                                    │
└─────────────────────────────────────────────────────────┘
```

### 2.2 协议层切面关键接口

**文件**：`internal/duo/types.go:14-24`

```go
// BaseProvider 描述上游厂商的签名调用能力
type BaseProvider interface {
    SignedCall(method string, uri string, params url.Values, options ...duoapi.DuoApiOption) (*http.Response, []byte, error)
}

// Provider 描述业务层使用的能力
type Provider interface {
    Call(ctx middlewares.Context, userSession *session.UserSession, values url.Values, method string, path string) (*Response, error)
    PreAuthCall(ctx middlewares.Context, userSession *session.UserSession, values url.Values) (*PreAuthResponse, error)
    AuthCall(ctx middlewares.Context, userSession *session.UserSession, values url.Values) (*AuthResponse, error)
}
```

**设计要点**：
- `BaseProvider` 是对底层 `duo_api_golang` 库的抽象，仅负责签名 HTTP 调用
- `Provider` 是业务层接口，封装了 PreAuth 和 Auth 两种业务语义
- 两层接口之间通过 `Call` 方法进行桥接，统一处理响应解析和错误转换

---

## 3. 完整调用流程

### 3.1 流程总览

```
用户发起 Duo 认证请求
        │
        ▼
DuoPOST 入口 (handler_sign_duo.go:47)
        │
        ├─► 检查是否有首选设备 (StorageProvider)
        │       │
        │       ├─ 有首选设备 ──► HandlePreferredDeviceCheck
        │       │                     │
        │       │                     └─► DuoPreAuth (预校验)
        │       │
        │       └─ 无首选设备 ──► HandleInitialDeviceSelection
        │                             │
        │                             └─► DuoPreAuth (预校验)
        │
        ▼
  预校验结果处理
        │
        ├─ enroll ──► 返回注册 URL，流程终止
        ├─ deny   ──► 返回拒绝，流程终止
        ├─ allow  ──► 直接通过，更新会话，流程终止
        └─ auth   ──► 继续设备选择
                      │
                      ▼
                PerformDuoAuthentication (正式签名)
                      │
                      ├─► AuthCall (正式签名调用)
                      │
                      ├─ 成功 ──► HandleAllow ──► 更新会话状态
                      └─ 失败 ──► 记录认证日志，返回错误
```

### 3.2 预校验（PreAuth）调用链

**入口**：`internal/handlers/handler_sign_duopreauth.go:13`

```go
func DuoPreAuth(ctx *middlewares.AutheliaCtx, userSession *session.UserSession, duoAPI duo.Provider) (result, message string, devices []DuoDevice, enrollURL string, err error) {
    values := url.Values{}
    values.Set("username", userSession.Username)
    
    preAuthResponse, err := duoAPI.PreAuthCall(ctx, userSession, values)
    // ... 结果过滤与转换
}
```

**底层调用**：`internal/duo/duo.go:61-74`

```go
func (d *Production) PreAuthCall(ctx middlewares.Context, userSession *session.UserSession, values url.Values) (r *PreAuthResponse, err error) {
    response, err := d.Call(ctx, userSession, values, fasthttp.MethodPost, "/auth/v2/preauth")
    // ... 解析 PreAuthResponse
}
```

**预校验的作用**：
1. 验证用户是否在 Duo 系统中注册
2. 获取用户可用的设备列表和能力
3. 检查是否需要强制注册或拒绝访问
4. 为后续正式签名调用选择合适的设备和方法

### 3.3 正式签名（Auth）调用链

**入口**：`internal/handlers/handler_sign_duo.go:71`

```go
func PerformDuoAuthentication(ctx *middlewares.AutheliaCtx, userSession *session.UserSession, duoAPI duo.Provider, device, method, remoteIP string, bodyJSON *bodySignDuoRequest) error {
    values, err := SetValues(*userSession, device, method, remoteIP, bodyJSON.TargetURL, bodyJSON.Passcode)
    
    authResponse, err := duoAPI.AuthCall(ctx, userSession, values)
    if authResponse.Result != allow {
        doMarkAuthenticationAttempt(ctx, false, ...)
        return fmt.Errorf("duo authentication failed")
    }
    
    doMarkAuthenticationAttempt(ctx, true, ...)
    return nil
}
```

**底层调用**：`internal/duo/duo.go:77-90`

```go
func (d *Production) AuthCall(ctx middlewares.Context, userSession *session.UserSession, values url.Values) (r *AuthResponse, err error) {
    response, err := d.Call(ctx, userSession, values, fasthttp.MethodPost, "/auth/v2/auth")
    // ... 解析 AuthResponse
}
```

---

## 4. 两次调用之间的状态衔接

### 4.1 状态传递载体

| 载体类型 | 存储位置 | 传递内容 | 生命周期 |
|---------|---------|---------|---------|
| `session.UserSession` | Cookie 会话 | 用户身份、认证级别 | 跨请求持久 |
| 内存变量 | Handler 栈帧 | 选中的 device、method | 单次请求内 |
| `StorageProvider` | 数据库 | 首选设备偏好 | 长期持久 |
| `url.Values` | HTTP 参数 | 单次调用的参数 | 单次调用 |

### 4.2 关键状态衔接点

**文件**：`internal/handlers/handler_sign_duo.go:129-146`

```go
func HandleDuoAuthentication(ctx *middlewares.AutheliaCtx, userSession *session.UserSession, duoAPI duo.Provider, bodyJSON *bodySignDuoRequest) error {
    // 步骤1: 从持久化存储加载首选设备
    duoDevice, err := ctx.Providers.StorageProvider.LoadPreferredDuoDevice(ctx, userSession.Username)
    
    if err != nil {
        // 步骤2a: 无首选设备，发起预校验
        selectedDevice, selectedMethod, err := HandleInitialDeviceSelection(ctx, userSession, duoAPI, bodyJSON)
        // 步骤3a: 使用预校验返回的设备信息进行正式签名
        return PerformDuoAuthenticationIfValid(ctx, userSession, duoAPI, selectedDevice, selectedMethod, bodyJSON)
    }
    
    // 步骤2b: 有首选设备，发起预校验验证有效性
    selectedDevice, selectedMethod, err := HandlePreferredDeviceCheck(ctx, userSession, duoAPI, duoDevice.Device, duoDevice.Method, bodyJSON)
    // 步骤3b: 使用验证后的设备信息进行正式签名
    return PerformDuoAuthenticationIfValid(ctx, userSession, duoAPI, selectedDevice, selectedMethod, bodyJSON)
}
```

### 4.3 会话推进机制

**成功路径的会话更新**：`internal/handlers/handler_sign_duo.go:325-352`

```go
func HandleAllow(ctx *middlewares.AutheliaCtx, userSession *session.UserSession, bodyJSON *bodySignDuoRequest) {
    // 1. 重新生成会话ID（安全最佳实践）
    if err = ctx.RegenerateSession(); err != nil {
        // ... 错误处理
    }
    
    // 2. 更新会话中的认证状态
    userSession.SetTwoFactorDuo(ctx.GetClock().Now())
    
    // 3. 持久化会话
    if err = ctx.SaveSession(*userSession); err != nil {
        // ... 错误处理
    }
    
    // 4. 决定响应方式（普通重定向或 OIDC 流程）
    if len(bodyJSON.Flow) > 0 {
        handleFlowResponse(ctx, userSession, bodyJSON.FlowID, bodyJSON.Flow, bodyJSON.SubFlow, bodyJSON.UserCode)
    } else {
        Handle2FAResponse(ctx, bodyJSON.TargetURL)
    }
}
```

**会话状态更新**：`internal/session/user_session.go:79-83`

```go
func (s *UserSession) SetTwoFactorDuo(now time.Time) {
    s.setTwoFactor(now)
    s.AuthenticationMethodRefs.Duo = true
}

func (s *UserSession) setTwoFactor(now time.Time) {
    s.SecondFactorAuthnTimestamp = now.Unix()
    s.LastActivity = now.Unix()
}
```

---

## 5. 失败分支分析

### 5.1 预校验阶段失败场景

| 失败场景 | 处理方式 | 会话影响 |
|---------|---------|---------|
| HTTP 网络错误 | 返回 `messageMFAValidationFailed` | 无变化 |
| Duo 返回 `enroll` | 返回注册 URL，前端引导用户注册 | 无变化 |
| Duo 返回 `deny` | 返回拒绝状态 | 无变化 |
| Duo 返回 `allow` | **直接通过**，更新会话为 2FA 状态 | 会话升级 |
| 无可用设备 | 返回 `enroll` 状态 | 无变化 |
| 首选设备失效 | 删除存储的首选设备，返回设备选择 | 持久化状态清理 |

**关键代码**：`internal/handlers/handler_sign_duo.go:149-180`

```go
func HandleDuoPreAuthResult(ctx *middlewares.AutheliaCtx, userSession *session.UserSession, result, message string, devices []DuoDevice, enrollURL string, bodyJSON *bodySignDuoRequest) (device, method string, err error) {
    switch result {
    case enroll:
        // 用户未注册，返回注册URL
        ctx.SetJSONBody(DuoSignResponse{Result: enroll, EnrollURL: enrollURL})
        return "", "", nil
        
    case deny:
        // 用户被拒绝
        ctx.SetJSONBody(DuoSignResponse{Result: deny})
        return "", "", nil
        
    case allow:
        // 特殊情况：Duo 直接允许，绕过设备选择
        HandleAllow(ctx, userSession, bodyJSON)
        return "", "", nil
        
    case auth:
        // 需要继续认证流程
        return HandleAuthResult(ctx, userSession, devices, "", "")
    }
}
```

### 5.2 正式签名阶段失败场景

| 失败场景 | 处理方式 | 会话影响 |
|---------|---------|---------|
| HTTP 网络错误 | 返回认证失败 | 无变化 |
| Duo 返回非 `allow` | 记录失败认证日志 | 无变化 |
| 会话再生失败 | 返回认证失败 | 无变化 |
| 会话保存失败 | 返回认证失败 | 状态可能不一致 |

**关键代码**：`internal/handlers/handler_sign_duo.go:86-97`

```go
if authResponse.Result != allow {
    doMarkAuthenticationAttempt(ctx, false, regulation.NewBan(...), regulation.AuthTypeDuo,
        fmt.Errorf("duo auth result: '%s', status: '%s', message: '%s'", 
            authResponse.Result, authResponse.Status, authResponse.StatusMessage))
    return fmt.Errorf("duo authentication failed")
}

doMarkAuthenticationAttempt(ctx, true, regulation.NewBan(...), regulation.AuthTypeDuo, nil)
```

### 5.3 失败分支的会话推进原则

1. **原子性**：预校验失败不影响会话状态，只有正式签名成功才更新会话
2. **幂等性**：多次失败尝试不会导致会话状态异常
3. **可观测性**：所有失败都通过 `doMarkAuthenticationAttempt` 记录审计日志
4. **安全性**：认证成功后必须调用 `RegenerateSession` 防止会话固定攻击

---

## 6. 协议层切面深度分析

### 6.1 统一签名调用机制

**文件**：`internal/duo/duo.go:22-58`

```go
func (d *Production) Call(ctx middlewares.Context, userSession *session.UserSession, values url.Values, method string, path string) (r *Response, err error) {
    // 所有 Duo API 调用都通过 SignedCall 进行签名
    if _, body, err = d.SignedCall(method, path, values); err != nil {
        return nil, fmt.Errorf("error occurred making signed call: %w", err)
    }
    
    // 统一响应解析
    if err = json.Unmarshal(body, &response); err != nil {
        return nil, fmt.Errorf("error occurred parsing response: %w", err)
    }
    
    // 统一状态码处理
    switch response.Stat {
    case "OK":
        return &response, nil
    case "FAIL":
        return &response, fmt.Errorf("failure status was returned")
    default:
        return &response, fmt.Errorf("unknown status was returned")
    }
}
```

### 6.2 切面设计优势

1. **关注点分离**：
   - 下层 `SignedCall` 只关心 HTTP 签名和传输
   - 上层 `PreAuthCall`/`AuthCall` 只关心业务语义和参数组装
   - 中间 `Call` 方法统一处理响应解析和错误转换

2. **可测试性**：
   - 通过 `BaseProvider` 接口可以轻松 Mock 底层 HTTP 调用
   - 通过 `Provider` 接口可以测试业务逻辑而不需要真实网络请求

3. **可扩展性**：
   - 新增其他第二因素厂商时，可以复用相同的层次结构
   - 统一的错误处理模式便于监控和告警

### 6.3 参数传递契约

**预校验参数**（`/auth/v2/preauth`）：
- `username`：用户名

**正式签名参数**（`/auth/v2/auth`）：
- `username`：用户名
- `ipaddr`：客户端 IP 地址
- `factor`：认证方法（push/phone/sms/mobile_otp）
- `device`：设备 ID（push/phone/sms 时需要）
- `passcode`：验证码（mobile_otp 时需要）
- `pushinfo`：推送附加信息（push 时可选）
- `display_username`：显示用户名（push 时可选）

---

## 7. 设计亮点与潜在改进点

### 7.1 设计亮点

1. **防御性编程**：预校验阶段验证首选设备有效性，避免使用过期设备信息
2. **自动降级**：首选设备失效时自动回退到手动选择流程
3. **安全加固**：认证成功后立即重新生成会话 ID
4. **审计追踪**：所有认证尝试（无论成功失败）都记录日志

### 7.2 潜在改进点

1. **状态机重构**：当前使用多层嵌套函数调用，可以考虑使用状态机模式使流程更清晰
2. **事务性**：会话更新和日志记录缺乏事务保证，极端情况下可能不一致
3. **超时控制**：两次远端调用各自独立超时，缺乏整体超时预算控制
4. **可观测性**：可以在协议层切面增加 metrics 埋点，便于监控厂商 API 可用性

---

## 8. 总结

Duo 第二因素认证的两次远端调用（PreAuth + Auth）通过以下机制实现状态衔接：

1. **分层架构**：策略评估层、协议切面层、HTTP 客户端层清晰分离
2. **状态载体**：会话（跨请求）、内存变量（单次请求）、持久化存储（长期）三级状态传递
3. **失败安全**：预校验失败不影响会话，只有正式签名成功才推进认证状态
4. **协议统一**：所有厂商调用都通过 `SignedCall` 进行签名，确保安全性和一致性

这种设计既保证了安全性（签名验证、会话再生），又提供了良好的用户体验（设备自动选择、错误降级），是典型的企业级认证系统架构实践。
