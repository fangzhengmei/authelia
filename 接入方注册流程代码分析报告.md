# Authelia 接入方注册流程代码分析报告

## 1. 概述

本报告分析 Authelia 项目中 OIDC (OpenID Connect) 接入方注册流程中的关键代码实现，包括客户端元数据验证、回调地址校验、动态注册与静态配置的区分，以及配置变更后的凭据兼容处理机制。

## 2. 注册接口字段验证

### 2.1 验证入口

验证流程入口位于 `internal/configuration/validator/identity_providers.go:22` 的 `ValidateIdentityProviders` 函数，该函数调用 `validateOIDC` 进行完整的 OIDC 配置验证。

### 2.2 客户端字段验证

核心客户端验证逻辑在 `validateOIDCClient` 函数（`identity_providers.go:668`）中实现，涵盖以下字段：

| 字段 | 验证规则 | 代码位置 |
|------|---------|---------|
| **client_id** | 长度 ≤ 100 字符，仅允许 RFC3986 非保留字符 | `identity_providers.go:628-647` |
| **client_name** | 可选，为空时默认使用 client_id | `identity_providers.go:636-638` |
| **client_secret** | 根据认证方法类型验证是否必需 | `identity_providers.go:768-774` |
| **authorization_policy** | 必须是已配置的授权策略 | `identity_providers.go:671-684` |
| **scopes** | 必须在支持的 scope 列表中，支持去重检查 | `identity_providers.go:961-1025` |
| **grant_types** | 必须在支持的授权类型列表中，与 response_types 匹配 | `identity_providers.go:1167-1189` |
| **response_types** | 必须在支持的响应类型列表中 | `identity_providers.go:1109-1128` |
| **response_modes** | 必须在支持的响应模式列表中 | `identity_providers.go:1131-1165` |
| **redirect_uris** | 必须是绝对 URL，公共客户端支持特殊 URN | `identity_providers.go:1253-1287` |

### 2.3 客户端 ID 验证细节

```go
// internal/configuration/validator/identity_providers.go:628-647
switch {
case n == 0:
    blankClientIDs = append(blankClientIDs, "#"+strconv.Itoa(c+1))
case n > 100:
    validator.Push(fmt.Errorf(errFmtOIDCClientIDTooLong, client.ID, n))
case !reRFC3986Unreserved.MatchString(client.ID):
    validator.Push(fmt.Errorf(errFmtOIDCClientIDInvalidCharacters, client.ID))
default:
    // 重复 ID 检查
    if utils.IsStringInSlice(client.ID, clientIDs) {
        // ...
    }
}
```

## 3. 动态注册与静态配置的区分处理

### 3.1 当前实现状态

**重要发现**：Authelia 当前版本**仅支持静态配置**的客户端注册，不支持 RFC7591 动态客户端注册协议。

### 3.2 静态配置客户端存储

客户端配置存储在内存中，通过 `MemoryClientStore` 实现：

```go
// internal/oidc/store.go:36-59
func NewMemoryClientStore(config *schema.Configuration) (store *MemoryClientStore) {
    store = &MemoryClientStore{
        clients: map[string]Client{},
    }

    // 从配置文件加载所有客户端
    for _, client := range config.IdentityProviders.OIDC.Clients {
        store.clients[client.ID] = NewClient(client, config.IdentityProviders.OIDC, policies)
    }

    return store
}
```

### 3.3 静态 vs 动态注册对比

| 特性 | 静态配置 | 动态注册 (RFC7591) |
|------|---------|-------------------|
| 配置方式 | YAML/TOML 配置文件 | HTTP API 端点 |
| 客户端存储 | 内存 map | 持久化存储 |
| 运行时修改 | 需重启服务 | 实时生效 |
| 注册端点 | 无 | `/connect/register` |
| 初始访问令牌 | 无 | 需要 |
| 适用场景 | 可信接入方 | 第三方开发者 |

### 3.4 配置发现元数据中的注册端点

在发现文档（Discovery Document）中，注册端点字段存在但未启用：

```go
// internal/oidc/types.go:483
RegistrationEndpoint string `json:"registration_endpoint,omitempty"`
```

## 4. 回调地址严格匹配实现细节

### 4.1 回调地址验证流程

回调地址验证分为两个层次：

1. **基本格式验证** - `validateOIDCClientRedirectURIs`（配置加载时）
2. **Sector Identifier 验证** - `ValidateSectorIdentifierURI`（运行时请求时）

### 4.2 基本格式验证

```go
// internal/configuration/validator/identity_providers.go:1253-1287
func validateOIDCClientRedirectURIs(c int, config *schema.IdentityProvidersOpenIDConnect, validator *schema.StructValidator, errDeprecatedFunc func()) {
    for _, redirectURI := range config.Clients[c].RedirectURIs {
        // 公共客户端允许特殊 URN: urn:ietf:wg:oauth:2.0:oob
        if redirectURI == oidc.RedirectURISpecialOAuth2InstalledApp {
            if config.Clients[c].Public {
                continue
            }
            validator.Push(fmt.Errorf(...))
            continue
        }

        // 必须是可解析的 URL
        if parsedRedirectURI, err = url.Parse(redirectURI); err != nil {
            validator.Push(fmt.Errorf(...))
            continue
        }

        // 必须是绝对 URL
        if !parsedRedirectURI.IsAbs() {
            validator.Push(fmt.Errorf(...))
        }
    }
}
```

### 4.3 Sector Identifier 严格匹配

当客户端配置了 `sector_identifier_uri` 时，系统会执行严格的回调地址验证：

```go
// internal/oidc/util.go:463-490
func ValidateSectorIdentifierURI(ctx ClientContext, cache map[string][]string, sectorURI *url.URL, redirectURIs []string) (err error) {
    // 1. 获取 Sector Identifier URI 中的回调地址列表
    sectorRedirectURIs, err = getSectorIdentifierURICache(ctx, cache, sectorURI)
    
    // 2. 对每个回调地址进行严格匹配检查
    for _, rawRedirectURI := range redirectURIs {
        if _, match := oauthelia2.IsMatchingRedirectURI(
            rawRedirectURI, 
            sectorRedirectURIs, 
            &oauthelia2.BestPracticeURIComparisonStrategy{},
        ); match {
            continue
        }
        invalidRedirectURIs = append(invalidRedirectURIs, rawRedirectURI)
    }
    
    // 3. 返回不匹配的地址
    if len(invalidRedirectURIs) > 0 {
        return fmt.Errorf("error checking redirect_uri '%s' against '%s'", ...)
    }
    return nil
}
```

### 4.4 BestPracticeURIComparisonStrategy 匹配策略

使用 `oauthelia2` 库的最佳实践 URI 比较策略，遵循 OAuth 2.0 安全最佳实践（RFC8252、OAuth 2.0 Security Best Current Practice）：

- **精确匹配**：Scheme、Host、Port、Path 必须完全一致
- **不允许通配符**：不支持 `*.example.com` 形式的通配符
- **区分大小写**：路径部分区分大小写
- **查询参数**：查询参数必须精确匹配
- **Fragment 忽略**：Fragment 部分在匹配时被忽略

### 4.5 测试用例验证

```go
// internal/oidc/util_test.go:320-390
// 测试用例展示了严格匹配行为：
// - 完全匹配: "https://app.example.com/callback" ✓
// - 路径不匹配: "https://app.example.com/cb1" vs "https://app.example.com/cb2" ✗
// - 主机不匹配: "https://app.example.com/callback" vs "https://other.example.com/callback" ✗
```

## 5. 配置变更后已发凭据的兼容处理机制

### 5.1 客户端密钥轮换

Authelia 支持通过 `RotatedClientSecrets` 机制实现密钥轮换，确保配置变更后已颁发的凭据仍然有效：

```go
// internal/oidc/types.go:62-63
type RegisteredClient struct {
    ClientSecret         *ClientSecretDigest    // 当前密钥
    RotatedClientSecrets []*ClientSecretDigest // 已轮换的历史密钥
}

// internal/oidc/client.go:159-168
func (c *RegisteredClient) GetRotatedClientSecrets() (secrets []oauthelia2.ClientSecret) {
    secrets = make([]oauthelia2.ClientSecret, len(c.RotatedClientSecrets))
    for i, secret := range c.RotatedClientSecrets {
        secrets[i] = secret
    }
    return secrets
}
```

### 5.2 全局密钥轮换

除了客户端级别的密钥轮换，还支持全局 HMAC 密钥轮换：

```go
// internal/oidc/config.go:83-86
type Config struct {
    GlobalSecret         []byte     // 当前全局密钥
    RotatedGlobalSecrets [][]byte   // 已轮换的历史全局密钥
}

// internal/oidc/config.go:661-664
func (c *Config) GetRotatedGlobalSecrets(ctx context.Context) (secrets [][]byte, err error) {
    return c.RotatedGlobalSecrets, nil
}
```

### 5.3 凭据生命周期管理

#### 5.3.1 Token 生命周期

配置变更不影响已颁发 Token 的有效性，直到 Token 自然过期：

| Token 类型 | 默认有效期 | 配置位置 |
|-----------|-----------|---------|
| Access Token | 60 分钟 | `lifespans.access_token` |
| Refresh Token | 90 分钟 | `lifespans.refresh_token` |
| ID Token | 60 分钟 | `lifespans.id_token` |
| Authorize Code | 1 分钟 | `lifespans.authorize_code` |

#### 5.3.2 会话存储机制

已颁发的 Token 存储在数据库中，配置变更不会自动撤销：

```go
// internal/oidc/store.go:158-162
func (s *Store) CreateAccessTokenSession(ctx context.Context, signature string, request oauthelia2.Requester) (err error) {
    return s.saveSession(ctx, storage.OAuth2SessionTypeAccessToken, signature, request)
}
```

### 5.4 配置变更的影响范围

| 配置项变更 | 对已发凭据的影响 | 处理机制 |
|-----------|----------------|---------|
| **client_secret** | 旧密钥失效但可通过轮换兼容 | `RotatedClientSecrets` |
| **redirect_uris** | 使用旧回调地址的请求被拒绝 | 无兼容，需重新授权 |
| **scopes** | 已颁发 Token 的 scope 不变 | 新 Token 使用新配置 |
| **grant_types** | 已颁发 Token 仍可使用 | 新授权请求受限制 |
| **signing_alg** | 已颁发 Token 仍可验证 | 新 Token 使用新算法 |
| **jwks** | 旧密钥可用于验证旧 Token | 密钥轮换机制 |

### 5.5 令牌验证的向后兼容性

在 Token 验证时，系统会尝试所有可用的密钥：

1. 首先使用当前密钥验证
2. 如果失败，尝试轮换的历史密钥
3. 全部失败才返回验证错误

## 6. 关键代码文件索引

| 功能模块 | 文件路径 | 核心函数 |
|---------|---------|---------|
| 客户端结构体 | `internal/oidc/types.go:58-141` | `RegisteredClient` |
| 客户端创建 | `internal/oidc/client.go:20-109` | `NewClient` |
| 客户端存储 | `internal/oidc/store.go:27-59` | `NewMemoryClientStore` |
| 配置验证 | `internal/configuration/validator/identity_providers.go` | `validateOIDCClient` |
| 回调地址验证 | `internal/oidc/util.go:463-490` | `ValidateSectorIdentifierURI` |
| 密钥轮换 | `internal/oidc/client.go:159-168` | `GetRotatedClientSecrets` |

## 7. 总结

### 7.1 核心设计特点

1. **静态配置优先**：当前版本仅支持静态配置客户端，确保接入方可信性
2. **严格安全校验**：回调地址采用精确匹配策略，遵循 OAuth 2.0 安全最佳实践
3. **密钥轮换支持**：通过 `RotatedClientSecrets` 实现平滑密钥轮换
4. **配置驱动验证**：所有字段验证在配置加载时完成，减少运行时开销

### 7.2 安全设计考量

- 不支持通配符回调地址，防止重定向攻击
- 客户端 ID 字符集限制，防止注入攻击
- 密钥长度和算法强制验证
- Sector Identifier 机制确保跨域回调安全

### 7.3 限制与改进空间

1. **缺乏动态注册**：当前不支持 RFC7591 动态客户端注册
2. **配置变更需重启**：静态配置变更需要服务重启才能生效
3. **无令牌主动撤销**：配置变更后已发 Token 不会被主动撤销，需等待自然过期
