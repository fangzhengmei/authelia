# 首条命中即返回判定机制与交互边界分析

## 一、首条命中即返回机制的源码实现

### 1.1 核心循环

`authorizer.go:51-68`：

```go
func (p *Authorizer) GetRequiredLevel(subject Subject, object Object) (hasSubjects bool, level Level) {
    for _, rule := range p.rules {
        if rule.IsMatch(subject, object) {
            return rule.HasSubjects, rule.Policy   // ★ 首条命中立即返回
        }
    }
    return false, p.defaultPolicy                   // 无命中则用默认策略
}
```

**关键特性**：
- 规则按配置顺序遍历（`Position` 从 1 开始）
- **一旦 `IsMatch` 返回 `true`，立即返回该规则的 `HasSubjects` 和 `Policy`**
- 后续规则不再检查——即使后面的规则更精确或更严格
- 无命中时应用 `defaultPolicy`（默认 `deny`）

### 1.2 IsMatch 的全条件 AND 语义

`access_control_rule.go:54-80`：

```go
func (acr *AccessControlRule) IsMatch(subject Subject, object Object) (match bool) {
    if !acr.MatchesDomains(subject, object)   { return false }
    if !acr.MatchesResources(subject, object) { return false }
    if !acr.MatchesQuery(object)              { return false }
    if !acr.MatchesMethods(object)            { return false }
    if !acr.MatchesNetworks(subject)          { return false }
    if !acr.MatchesSubjects(subject)          { return false }
    return true
}
```

六个维度全部通过才命中。**每个维度内部是 OR 语义**（如多个域名任一匹配即可），**维度之间是 AND 语义**。

### 1.3 单维度内部的 OR 短路

每个维度内的多个条件只要一个匹配即可，且同样采用短路求值：

```go
// 域名维度: 多个域名 OR
func (acr *AccessControlRule) MatchesDomains(subject Subject, object Object) (matches bool) {
    if len(acr.Domains) == 0 { return true }
    for _, domain := range acr.Domains {
        if domain.IsMatch(subject, object) { return true }   // ★ 首个匹配即返回
    }
    return false
}

// 主体维度: 多个主体规则组 OR
func (acr *AccessControlRule) MatchesSubjectExact(subject Subject) (match bool) {
    if len(acr.Subjects) == 0 { return true }
    else if subject.IsAnonymous() { return false }
    for _, subjectRule := range acr.Subjects {
        if subjectRule.IsMatch(subject) { return true }      // ★ 首个匹配即返回
    }
    return false
}
```

### 1.4 规则匹配的完整短路链

```
请求进入 → for each rule (按 Position 顺序):
    │
    ├─ MatchesDomains?
    │   └─ for each domain: 首个匹配 → true; 全不匹配 → false → 跳过此规则
    │
    ├─ MatchesResources?
    │   └─ for each resource: 首个匹配 → true; 全不匹配 → false → 跳过此规则
    │
    ├─ MatchesQuery? → false → 跳过
    ├─ MatchesMethods? → false → 跳过
    ├─ MatchesNetworks? → false → 跳过
    │
    └─ MatchesSubjects?
        ├─ IsAnonymous()=true → true (★ 匿名短路)
        └─ MatchesSubjectExact()
            └─ for each subjectRule: 首个匹配 → true; 全不匹配 → false → 跳过此规则
    │
    全部通过 → return (rule.HasSubjects, rule.Policy)  ★ 首条命中即返回
```

---

## 二、首条命中与动态匹配的交互边界

### 2.1 宽域名规则前置覆盖窄规则的典型场景

**错误配置示例**：

```yaml
access_control:
  rules:
    - domain: "*.example.com"         # 规则1: 宽域名
      policy: one_factor

    - domain: "{user}.example.com"    # 规则2: 窄域名（需要用户身份匹配）
      policy: two_factor

    - domain: "admin.example.com"     # 规则3: 精确域名
      subject: "group:admins"
      policy: two_factor
```

**分析**：

| 请求者 | 目标域名 | 命中规则 | 策略 | 预期策略 |
|--------|---------|---------|------|---------|
| John(已认证) | john.example.com | **规则1** | one_factor | two_factor (规则2) |
| John(已认证) | admin.example.com | **规则1** | one_factor | two_factor (规则3) |
| 匿名用户 | john.example.com | **规则1** | one_factor | 应先登录 |
| ClientID=app | john.example.com | **规则1** | one_factor | 应拒绝 |

`*.example.com` 匹配所有子域名（包括 `{user}.example.com` 和 `admin.example.com`），因为通配符匹配是**纯后缀匹配**，不涉及任何身份信息。首条命中即返回使得规则2和规则3**永远不会被评估**。

### 2.2 {user}/{group} 域名匿名放宽与首条命中的交互

**问题配置**：

```yaml
access_control:
  rules:
    - domain: "{user}.example.com"    # 规则1: 动态域名 + one_factor
      policy: one_factor

    - domain: "{user}.example.com"    # 规则2: 同域名 + deny（意图拒绝非本人）
      subject: "user:john"            # 但此规则永远不会被评估！
      policy: deny
```

**对 John 访问 bob.example.com**：
- 规则1：`{user}.example.com` 匹配 → `domain="bob.example.com"`，`subject.Username="john"` → `Username != "bob"` → 域名匹配**失败**（已认证用户走精确匹配）
- 规则2：`{user}.example.com` 匹配 → `subject: ["user:john"]` → `Username == "john"` 但域名 `bob.example.com` ≠ `john.example.com` → 域名匹配**失败**
- 结果：命中 defaultPolicy

**对匿名用户访问 bob.example.com**：
- 规则1：`{user}.example.com` → `IsAnonymous()=true` → 子域非空即匹配 → **命中** → `one_factor`
- 规则2：**永远不会被评估**
- 结果：匿名用户获得 `one_factor` 提示而非直接拒绝

**对 Client Credentials 访问 bob.example.com**：
- 规则1：`{user}.example.com` → `IsAnonymous()=true` → 子域非空即匹配 → **命中** → `one_factor`
- 规则2：永远不会被评估
- 结果：CC 请求匹配 `one_factor`，因 `Level=OneFactor` → **直接授权**

### 2.3 规则内部域名 OR 与首条命中的叠加

一条规则可以有多个域名，内部是 OR 关系：

```yaml
- domain:
    - "{user}.example.com"
    - "shared.example.com"
  policy: one_factor
```

对匿名用户访问 `shared.example.com`：
- 域名1 `{user}.example.com`：`IsAnonymous()=true`，`shared` 后缀匹配且非空 → **匹配**
- 域名2 `shared.example.com`：精确匹配 → 也匹配
- 但域名1 的匿名放宽已经短路返回 true，不会检查域名2
- 整条规则命中 → `one_factor`

**问题**：匿名用户访问 `shared.example.com` 时，域名1 的匿名放宽先命中了，但语义上这个请求应该由域名2 的精确匹配处理。实际上两种匹配的结果相同（都是 one_factor），但如果策略不同就会有差异。

### 2.4 RegexpGroupStringSubjectMatcher 的匿名放宽与首条命中

```yaml
- domain_regex: "^(?P<User>[a-z]+)\\.example\\.com$"
  policy: one_factor
```

对匿名用户访问 `john.example.com`：
- 正则匹配成功 → `matches = ["john.example.com", "john"]`
- `IsAnonymous()=true` → **直接返回 true**
- 整条规则命中 → `one_factor`

关键：匿名放宽在正则匹配内部发生，是**正则匹配成功的前提下的放宽**。如果正则本身不匹配（如 `xyz.other.com`），则不会触发匿名放宽。这与 `{user}` 简单域名不同——简单域名只检查后缀和子域非空，而正则需要先满足模式。

---

## 三、首条命中与 oauth2:client 主体约束的交互边界

### 3.1 致命交互：匿名短路使 oauth2:client 规则不可达

```yaml
access_control:
  rules:
    - domain: "api.example.com"
      subject: "oauth2:client:trusted"
      policy: bypass

    - domain: "api.example.com"
      policy: deny
```

**ClientID=trusted 的 CC 请求**：
- 规则1：域名匹配 ✓ → `MatchesSubjects()` → `IsAnonymous()=true` → **返回 true** → 命中 → bypass
- ✓ 结果正确（但原因错误：是匿名短路而非 ClientID 匹配）

**ClientID=malicious 的 CC 请求**：
- 规则1：域名匹配 ✓ → `MatchesSubjects()` → `IsAnonymous()=true` → **返回 true** → 命中 → bypass
- ✗ **安全漏洞**：恶意客户端获得了 bypass 授权

**规则2 的 deny 永远不会被 CC 请求评估**，因为规则1 的匿名短路已经拦截了所有 CC 请求。

### 3.2 主体维度内部的 OR 短路与 oauth2:client

```yaml
- domain: "api.example.com"
  subject:
    - "group:admins"               # 主体组1
    - "oauth2:client:admin-bot"    # 主体组2（OR 关系）
  policy: bypass
```

主体规则组之间是 OR 关系。对 CC 请求：

```
MatchesSubjectExact(subject):
    subject.IsAnonymous() → true → return false  ★
```

`MatchesSubjectExact` 在 `IsAnonymous()=true` 时直接返回 `false`——**这与 `MatchesSubjects` 的行为不同**！

关键区别：
- `MatchesSubjects`：`IsAnonymous()=true` → **返回 true**（宽松模式）
- `MatchesSubjectExact`：`IsAnonymous()=true` → **返回 false**（严格模式）

但在 `GetRequiredLevel` 的主循环中，调用的是 `MatchesSubjects`（宽松模式），`MatchesSubjectExact` 只在 `GetRuleMatchResults` 的诊断方法中使用。

**因此**：CC 请求在 `GetRequiredLevel` 中**永远不会进入 `MatchesSubjectExact`**，所有含 `subject` 约束的规则对 CC 请求都因为匿名短路而匹配。

### 3.3 规则重排无法解决 oauth2:client 问题

考虑将更严格的规则前置：

```yaml
# 方案A：deny 规则前置
- domain: "api.example.com"
  policy: deny

- domain: "api.example.com"
  subject: "oauth2:client:trusted"
  policy: bypass
```

结果：所有对 `api.example.com` 的请求（包括 CC 请求）都命中规则1 → **全部被拒绝**。`oauth2:client:trusted` 的 bypass 规则永远不会被评估。

```yaml
# 方案B：有主体的规则前置
- domain: "api.example.com"
  subject: "oauth2:client:trusted"
  policy: bypass

- domain: "api.example.com"
  policy: deny
```

结果：CC 请求在规则1 因匿名短路匹配 → bypass。**问题未解决**。

**结论**：在 `IsAnonymous()` 不检查 ClientID 的前提下，**任何规则排列都无法让 `oauth2:client` 规则正确生效**。因为排列只影响"哪条规则被命中"，而不影响"命中时的判定逻辑"。

---

## 四、首条命中与 bypass 验证策略的交互边界

### 4.1 bypass 验证器的设计意图

`access_control.go:64-75`：

```go
func validateBypass(rulePosition int, rule schema.AccessControlRule, validator *schema.StructValidator) {
    // 1. bypass + 显式 subject → 禁止
    if len(rule.Subjects) != 0 {
        validator.Push(fmt.Errorf(...))
    }

    // 2. bypass + (?P<User/Group>) 正则域名 → 禁止
    for _, pattern := range rule.DomainsRegex {
        if utils.IsStringSliceContainsAny(authorization.IdentitySubexpNames, pattern.SubexpNames()) {
            validator.Push(fmt.Errorf(...))
            return
        }
    }
}
```

验证器的设计意图：**bypass 策略不应与任何需要身份信息的条件组合**，因为 bypass 意味着"无需认证即可访问"，而主体约束意味着"需要认证才能判断身份"——两者语义矛盾。

### 4.2 验证器的遗漏与首条命中的交互

**遗漏1：bypass + `{user}`/`{group}` 简单域名**

```yaml
- domain: "{user}.example.com"   # 通过验证！
  policy: bypass
```

验证器只检查 `rule.Subjects` 和 `rule.DomainsRegex`，**不检查 `rule.Domains` 中的 `{user}`/`{group}` 前缀**。

**与首条命中的交互**：如果此规则位于规则列表前端，所有对 `{user}.example.com` 子域名的请求（包括匿名和 CC）都会命中此 bypass 规则，后续任何更严格的规则都不会被评估。

**遗漏2：bypass + `(?P<User/Group>)` 资源正则**

```yaml
- domain: "app.example.com"
  resources: ["^/(?P<User>[a-z]+)/data$"]   # 通过验证！
  policy: bypass
```

验证器不检查 `rule.Resources` 中的命名组。

**与首条命中的交互**：对 `app.example.com/john/data` 的请求，匿名用户因正则匹配+匿名放宽命中 bypass，直接授权。

### 4.3 bypass 验证遗漏与宽规则前置的叠加风险

```yaml
access_control:
  rules:
    - domain: "{user}.example.com"     # 规则1: bypass + {user} (通过验证)
      policy: bypass

    - domain: "admin.example.com"      # 规则2: 严格规则（永远不会被评估）
      subject: "group:admins"
      policy: two_factor
```

匿名用户访问 `admin.example.com`：
- 规则1：`{user}.example.com` → `IsAnonymous()=true` → 后缀匹配 + 子域非空 → **匹配** → bypass
- 规则2：**永远不会被评估**

`admin` 作为 `{user}` 子域名被规则1 的匿名放宽拦截了。管理员页面的 two_factor 保护被绕过。

### 4.4 bypass 规则与默认策略的兜底交互

```go
// handler_authz.go:202-219
if err != nil {
    if !ruleHasSubject && required != authorization.Bypass {
        // 认证错误 + 无主体约束 + 非bypass → 处理未认证
        switch {
        case strategy == nil:
            ctx.ReplyUnauthorized()
        case strategy.HeaderStrategy():
            strategy.HandleUnauthorized(...)
            return
        }
    }
    // bypass规则时忽略认证错误
    ctx.GetLogger().WithError(err).Debug("...matched rule was a bypass rule")
}
```

**当认证过程出错但命中了 bypass 规则时**：
- `required == authorization.Bypass` → 不进入错误处理分支
- 继续执行 `isAuthzResult` → `Bypass` → `AuthzResultAuthorized`
- **即使认证出错，bypass 规则仍然放行**

这意味着：如果宽 bypass 规则前置，即使后端认证服务异常，请求也会被放行。

---

## 五、配置重排与收敛方式

### 5.1 核心原则：严格规则前置

首条命中机制要求**约束更严格的规则必须排在更前面**，否则会被宽规则"遮挡"。规则的严格程度由以下因素决定：

**维度严格度排序**（从严格到宽松）：

| 维度 | 严格 | → | 宽松 |
|------|------|---|------|
| 域名 | 精确域名 `admin.example.com` | `{user}`/`{group}` 动态域名 | 通配符 `*.example.com` |
| 资源 | `(?P<User>...)` 正则 | 精确路径 | `.*` 通配正则 |
| 主体 | `user:xxx` / `group:xxx` / `oauth2:client:xxx` | 无主体约束 | - |
| 网络 | 具体 IP/CIDR | 无网络约束 | - |
| 方法 | 具体方法 | 无方法约束 | - |

### 5.2 安全的规则排列模式

**模式 A：精确域名 → 动态域名 → 通配符域名**

```yaml
access_control:
  rules:
    # 1. 最精确：特定域名 + 特定主体
    - domain: "admin.example.com"
      subject: "group:admins"
      policy: two_factor

    # 2. 较精确：动态域名（{user} 会做身份匹配）
    - domain: "{user}.example.com"
      policy: two_factor

    # 3. 较宽松：通配符域名
    - domain: "*.example.com"
      policy: one_factor

    # 4. 兜底：默认策略
  default_policy: deny
```

**对 John(已认证, admins组) 访问 admin.example.com**：
- 规则1：域名精确匹配 ✓，主体匹配 ✓ → two_factor ✓

**对 John(已认证, users组) 访问 admin.example.com**：
- 规则1：域名精确匹配 ✓，主体不匹配 ✗ → 跳过
- 规则2：`{user}.example.com`，`admin` ≠ `john` → 域名匹配失败 → 跳过
- 规则3：`*.example.com` → 匹配 → one_factor ✓

**对匿名用户访问 admin.example.com**：
- 规则1：域名匹配 ✓，`MatchesSubjects()` → `IsAnonymous()=true` → **true** → two_factor
- isAuthzResult：`NotAuthenticated` < `TwoFactor` → Unauthorized → 重定向登录 ✓

### 5.3 不安全的规则排列与修正

**不安全排列**：

```yaml
# ✗ 通配符前置，遮挡所有精确规则
- domain: "*.example.com"
  policy: one_factor           # 所有子域名都命中此规则

- domain: "admin.example.com"
  subject: "group:admins"
  policy: two_factor           # 永远不会被评估
```

**修正**：将精确规则前置

```yaml
# ✓ 精确规则前置
- domain: "admin.example.com"
  subject: "group:admins"
  policy: two_factor

- domain: "*.example.com"
  policy: one_factor
```

### 5.4 {user}/{group} 与通配符的交互排列

**不安全排列**：

```yaml
# ✗ 通配符前置遮挡 {user} 规则
- domain: "*.example.com"
  policy: one_factor

- domain: "{user}.example.com"
  policy: two_factor           # 永远不会被评估
```

`*.example.com` 匹配所有子域名包括 `john.example.com`，`{user}.example.com` 永远不会被评估。

**修正**：动态域名前置

```yaml
# ✓ 动态域名前置
- domain: "{user}.example.com"
  policy: two_factor

- domain: "*.example.com"
  policy: one_factor
```

但注意：对于匿名/CC 请求，`{user}.example.com` 的匿名放宽仍会命中。需要额外的 deny 规则处理：

```yaml
# ✓ 完整修正
- domain: "{user}.example.com"
  policy: two_factor           # 已认证用户走身份匹配

- domain: "*.example.com"      # 匿名/CC 请求被此规则兜底
  policy: one_factor           # 需要认证
```

匿名用户访问 `john.example.com`：
- 规则1：域名匿名放宽 ✓，主体匿名短路 ✓ → 命中 two_factor
- isAuthzResult：`NotAuthenticated` < `TwoFactor` → Unauthorized → 重定向登录 ✓

### 5.5 oauth2:client 规则的不可排列修正

**无法通过重排修正的配置**：

```yaml
# 无论怎么排列，CC 请求的 IsAnonymous()=true 都会导致匿名短路
- domain: "api.example.com"
  subject: "oauth2:client:trusted"
  policy: bypass

- domain: "api.example.com"
  policy: deny
```

**唯一可行的配置收敛方案**：

#### 方案1：不使用 oauth2:client 主体，改用网络隔离

```yaml
- domain: "api.example.com"
  networks: "10.0.0.0/8"        # 用网络范围限制，而非 ClientID
  policy: bypass
```

此方案通过 IP 范围间接实现客户端隔离，绕过 `IsAnonymous()` 的 Bug。

#### 方案2：不使用 bypass，使用 one_factor + deny 兜底

```yaml
- domain: "api.example.com"
  policy: one_factor             # 所有请求需要认证
```

CC 请求的 `Level=OneFactor >= OneFactor` → Authorized。虽然所有 CC 请求都被授权，但至少没有 bypass 级别的风险。

#### 方案3：使用 two_factor 阻止 CC 请求

```yaml
- domain: "api.example.com"
  subject: "oauth2:client:trusted"
  policy: two_factor             # CC 的 OneFactor < TwoFactor → Unauthorized
```

CC 请求无法满足 `TwoFactor` → 被拒绝。**这是当前代码下唯一能让 oauth2:client 规则对 CC 请求产生区分效果的方式**，但效果是"全部拒绝"而非"仅授权 trusted 客户端"。

#### 方案4：使用 deny + 无主体实现显式拒绝

```yaml
- domain: "api.example.com"
  policy: deny                   # 先拒绝所有

- domain: "api-internal.example.com"  # 用不同域名隔离
  subject: "oauth2:client:trusted"
  policy: bypass
```

通过域名隔离而非主体约束来实现访问控制，完全避开 `IsAnonymous()` Bug。

### 5.6 bypass 验证遗漏的收敛方案

#### 遗漏1：bypass + `{user}` 域名

```yaml
# ✗ 危险配置（通过验证但语义有误）
- domain: "{user}.example.com"
  policy: bypass
```

**收敛**：将 bypass 改为 one_factor

```yaml
# ✓ 安全配置
- domain: "{user}.example.com"
  policy: one_factor
```

匿名/CC 请求命中后因需要认证而被重定向，不会直接授权。

#### 遗漏2：bypass + `(?P<User>)` 资源

```yaml
# ✗ 危险配置（通过验证但语义有误）
- domain: "app.example.com"
  resources: ["^/(?P<User>[a-z]+)/data$"]
  policy: bypass
```

**收敛**：将 bypass 改为 one_factor 或 two_factor

```yaml
# ✓ 安全配置
- domain: "app.example.com"
  resources: ["^/(?P<User>[a-z]+)/data$"]
  policy: two_factor
```

### 5.7 规则收敛的通用策略

| 场景 | 不安全配置 | 收敛方式 | 原理 |
|------|-----------|---------|------|
| 通配符遮挡精确域名 | `*.example.com` 前置 | 精确域名前置 | 首条命中机制 |
| `{user}` 被 `*` 遮挡 | `*.example.com` 前置 | `{user}` 前置 | 首条命中机制 |
| bypass + `{user}` | `bypass` 策略 | 改用 `one_factor` | 消除匿名放宽风险 |
| bypass + `(?P<User>)` 资源 | `bypass` 策略 | 改用 `one_factor` | 消除匿名放宽风险 |
| oauth2:client + bypass | 任何排列 | 改用 `two_factor` 或域名隔离 | 绕过 IsAnonymous Bug |
| deny + 匿名放宽 | deny 规则 | 确认 HasSubjects=true | isAuthzResult 二次判定 |

---

## 六、完整交互边界总结图

```
规则1          规则2          规则3          defaultPolicy
  │              │              │               │
  ▼              ▼              ▼               ▼
┌─────────────────────────────────────────────────────────┐
│  首条命中即返回 (GetRequiredLevel)                        │
│                                                         │
│  规则1.IsMatch(subject, object)?                        │
│  ├─ Domains:   匹配?  ← {user}匿名放宽 / *.通配 / 精确   │
│  ├─ Resources: 匹配?  ← (?P<User>)匿名放宽 / 精确        │
│  ├─ Query:     匹配?                                    │
│  ├─ Methods:   匹配?                                    │
│  ├─ Networks:  匹配?                                    │
│  └─ Subjects:  匹配?                                    │
│      ├─ IsAnonymous()=true → true  ★                    │
│      │   (包括 CC 请求: Username="" Groups=[])           │
│      └─ MatchesSubjectExact()                           │
│          └─ AccessControlClient.IsMatch()               │
│              (CC 请求永远不会到达此处)                     │
│                                                         │
│  命中 → return (HasSubjects, Policy)                    │
│  未命中 → 检查规则2 → 规则3 → ... → defaultPolicy       │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  isAuthzResult(level, required, ruleHasSubject)         │
│                                                         │
│  Bypass → Authorized (无视 HasSubjects)                 │
│  Denied + (已认证 || !HasSubjects) → Forbidden           │
│  Denied + 匿名 + HasSubjects → Unauthorized ★           │
│  OneFactor/TwoFactor + 级别足够 → Authorized             │
│  OneFactor/TwoFactor + 级别不足 → Unauthorized           │
│                                                         │
│  ★ CC 请求的 level=OneFactor                            │
│    → Denied → Forbidden (不会 Unauthorized)             │
│    → OneFactor → Authorized (匿名短路导致误匹配)         │
│    → Bypass → Authorized (匿名短路 + bypass = 危险)     │
└─────────────────────────────────────────────────────────┘
```

---

## 七、关键结论

### 7.1 首条命中机制的三层交互

1. **规则间交互**：宽规则前置会遮挡窄规则，必须严格规则前置
2. **规则内交互**：匿名放宽（`IsAnonymous()=true`）使条件维度从6维降为5维（主体维度恒为 true）
3. **二次判定交互**：`isAuthzResult` 通过 `ruleHasSubject` 部分补偿匿名放宽，但对 bypass 策略无效

### 7.2 首条命中不可解决的问题

| 问题 | 规则重排能否解决 | 根因 |
|------|-----------------|------|
| 通配符遮挡精确域名 | ✓ 能 | 纯顺序问题 |
| `{user}` 被 `*` 遮挡 | ✓ 能 | 纯顺序问题 |
| bypass + `{user}` 匿名放行 | ✗ 不能 | 匹配逻辑问题，非顺序问题 |
| oauth2:client 匿名短路 | ✗ 不能 | `IsAnonymous()` 不检查 ClientID |
| bypass 验证遗漏 | ✗ 不能 | 验证器不完整 |

### 7.3 收敛的本质

规则收敛不只是排列问题，还需要确保**每条规则的匹配语义本身是正确的**。首条命中机制保证了"先到先得"的确定性，但也放大了单条规则语义错误的影响——一条语义不正确的规则被命中后，后续所有规则都不会被评估，错误无法被后续规则纠正。
