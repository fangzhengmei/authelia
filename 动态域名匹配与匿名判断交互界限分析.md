# {user}/{group} 动态匹配与匿名判断、OAuth2 Client 规则交互界限分析

## 一、{user}/{group} 动态域名的判定流程

### 1.1 编译阶段：从配置到匹配器

**简单域名占位符** (`access_control_domain.go:12-31`)：

```go
func NewAccessControlDomain(domain string) (subjects bool, rule AccessControlDomain) {
    m := &AccessControlDomainMatcher{}
    domain = strings.ToLower(domain)

    switch {
    case strings.HasPrefix(domain, "*."):
        m.Wildcard = true
        m.Name = domain[1:]      // "*.example.com" → Name=".example.com"
    case strings.HasPrefix(domain, "{user}"):
        m.UserWildcard = true
        m.Name = domain[6:]      // "{user}.example.com" → Name=".example.com"
    case strings.HasPrefix(domain, "{group}"):
        m.GroupWildcard = true
        m.Name = domain[7:]      // "{group}.example.com" → Name=".example.com"
    default:
        m.Name = domain           // "example.com" → Name="example.com"
    }

    return m.UserWildcard || m.GroupWildcard, AccessControlDomain{m}
}
```

**返回值 `subjects` 的语义**：当域名包含 `{user}` 或 `{group}` 时返回 `true`，表示此域名匹配器依赖主体身份。

**正则域名命名组** (`access_control_domain.go:35-52`)：

```go
func NewAccessControlDomainRegex(pattern regexp.Regexp) (subjects bool, rule AccessControlDomain) {
    var iuser, igroup = -1, -1

    for i, group := range pattern.SubexpNames() {
        switch group {
        case subexpNameUser:   // "User"
            iuser = i
        case subexpNameGroup:  // "Group"
            igroup = i
        }
    }

    if iuser != -1 || igroup != -1 {
        // 包含 (?P<User>...) 或 (?P<Group>...) → 使用带身份的匹配器
        return true, AccessControlDomain{RegexpGroupStringSubjectMatcher{pattern, iuser, igroup}}
    }

    // 不包含命名组 → 使用纯字符串匹配器
    return false, AccessControlDomain{RegexpStringSubjectMatcher{pattern}}
}
```

### 1.2 HasSubjects 标记的聚合逻辑

`util.go:79-113` 中三个函数分别将域名、正则域名、正则资源的 `subjects` 返回值聚合到 `rule.HasSubjects`：

```go
func ruleAddDomain(domainRules []string, rule *AccessControlRule) {
    for _, domainRule := range domainRules {
        subjects, r := NewAccessControlDomain(domainRule)
        rule.Domains = append(rule.Domains, r)
        if !rule.HasSubjects && subjects {
            rule.HasSubjects = true
        }
    }
}

func ruleAddDomainRegex(exps []regexp.Regexp, rule *AccessControlRule) {
    for _, exp := range exps {
        subjects, r := NewAccessControlDomainRegex(exp)
        rule.Domains = append(rule.Domains, r)
        if !rule.HasSubjects && subjects {
            rule.HasSubjects = true
        }
    }
}

func ruleAddResources(exps []regexp.Regexp, rule *AccessControlRule) {
    for _, exp := range exps {
        subjects, r := NewAccessControlResource(exp)
        rule.Resources = append(rule.Resources, r)
        if !rule.HasSubjects && subjects {
            rule.HasSubjects = true
        }
    }
}
```

**HasSubjects 的三种来源汇总**：

| 来源 | 配置示例 | subjects=true 条件 |
|------|---------|-------------------|
| 简单域名 | `domain: "{user}.example.com"` | 前缀为 `{user}` 或 `{group}` |
| 正则域名 | `domain_regex: "^(?P<User>...)\\.com$"` | 包含 `(?P<User>...)` 或 `(?P<Group>...)` |
| 正则资源 | `resources: ["^/(?P<User>...)/api$"]` | 包含 `(?P<User>...)` 或 `(?P<Group>...)` |
| 显式主体 | `subject: ["group:admins"]` | `len(Subjects) > 0` |

### 1.3 匹配阶段：三种域名匹配器的 IsMatch 逻辑

#### (A) AccessControlDomainMatcher（简单域名）

`access_control_domain.go:63-84`：

```go
func (m AccessControlDomainMatcher) IsMatch(domain string, subject Subject) (match bool) {
    switch {
    case m.Wildcard:
        return strings.HasSuffix(domain, m.Name)              // *.example.com → 后缀匹配
    case m.UserWildcard:
        if subject.IsAnonymous() && strings.HasSuffix(domain, m.Name) {
            return len(domain) > len(m.Name)                   // ★ 匿名：只要子域非空就匹配
        }
        return domain == fmt.Sprintf("%s%s", subject.Username, m.Name)  // 已认证：用户名精确匹配
    case m.GroupWildcard:
        if subject.IsAnonymous() && strings.HasSuffix(domain, m.Name) {
            return len(domain) > len(m.Name)                   // ★ 匿名：只要子域非空就匹配
        }
        i := strings.Index(domain, ".")
        return domain[i:] == m.Name && utils.IsStringInSliceFold(domain[:i], subject.Groups)  // 已认证：组名匹配
    default:
        return strings.EqualFold(domain, m.Name)               // 精确匹配
    }
}
```

**匿名用户的语义放宽**：`{user}.example.com` 对匿名用户来说，只要 `john.example.com` 满足后缀匹配且子域非空就返回 `true`，**不检查 Username 是否与子域匹配**。这是有意设计——匿名用户尚未有身份，无法做用户名匹配，先放宽让其命中，再由 `isAuthzResult` 做二次判定。

#### (B) RegexpGroupStringSubjectMatcher（带命名组的正则）

`regexp.go:18-37`：

```go
func (r RegexpGroupStringSubjectMatcher) IsMatch(input string, subject Subject) (match bool) {
    matches := r.Pattern.FindStringSubmatch(input)
    if matches == nil {
        return false                     // 正则不匹配 → 直接 false
    }

    if subject.IsAnonymous() {
        return true                      // ★ 匿名：正则匹配后直接返回 true
    }

    if r.SubexpNameUser != -1 && !strings.EqualFold(subject.Username, matches[r.SubexpNameUser]) {
        return false                     // 已认证：检查 Username 与捕获组是否一致
    }

    if r.SubexpNameGroup != -1 && !utils.IsStringInSliceFold(matches[r.SubexpNameGroup], subject.Groups) {
        return false                     // 已认证：检查捕获组是否在 Groups 中
    }

    return true
}
```

**匿名放宽模式完全一致**：`IsAnonymous()=true` 时，只要正则本身能匹配输入字符串，就返回 `true`，跳过所有 User/Group 身份验证。

#### (C) RegexpStringSubjectMatcher（纯正则，无命名组）

`regexp.go:50-52`：

```go
func (r RegexpStringSubjectMatcher) IsMatch(input string, _ Subject) (match bool) {
    return r.Pattern.MatchString(input)  // 不依赖 subject，纯字符串匹配
}
```

**无匿名放宽问题**：因为不涉及任何身份信息，自然不存在语义放宽。

---

## 二、正则资源命名组的判定流程

### 2.1 编译阶段

`access_control_resource.go:8-25`：

```go
func NewAccessControlResource(pattern regexp.Regexp) (subjects bool, rule AccessControlResource) {
    var iuser, igroup = -1, -1

    for i, group := range pattern.SubexpNames() {
        switch group {
        case subexpNameUser:
            iuser = i
        case subexpNameGroup:
            igroup = i
        }
    }

    if iuser != -1 || igroup != -1 {
        return true, AccessControlResource{RegexpGroupStringSubjectMatcher{pattern, iuser, igroup}}
    }

    return false, AccessControlResource{RegexpStringSubjectMatcher{pattern}}
}
```

**与域名正则完全同构**：使用相同的 `RegexpGroupStringSubjectMatcher` 和 `RegexpStringSubjectMatcher`。

### 2.2 匹配调用链

```
IsMatch(subject, object)
    ↓
MatchesResources(subject, object)
    ↓
for _, resource := range acr.Resources:
    resource.IsMatch(subject, object)
        ↓
    acl.Matcher.IsMatch(object.Path, subject)   ← 注意: 传入的是 object.Path，不是 object.Domain
        ↓
    RegexpGroupStringSubjectMatcher.IsMatch(path, subject)
        或 RegexpStringSubjectMatcher.IsMatch(path, _)
```

### 2.3 资源匹配与域名匹配的关键区别

| 维度 | 域名匹配 | 资源匹配 |
|------|---------|---------|
| 输入 | `object.Domain` (e.g. `john.example.com`) | `object.Path` (e.g. `/john/personal`) |
| User 检查 | `(?P<User>...)` 捕获组 vs `subject.Username` | 同 |
| Group 检查 | `(?P<Group>...)` 捕获组 vs `subject.Groups` | 同 |
| 匿名放宽 | `IsAnonymous()=true` → `return true` | 同 |
| HasSubjects 影响 | `subjects=true` → `rule.HasSubjects=true` | 同 |

两者在匿名放宽逻辑上完全一致，区别仅在于匹配的字符串不同（域名 vs 路径）。

---

## 三、动态匹配与 ClientID 的交互界限

### 3.1 Client Credentials 的 Subject 状态回顾

```go
Subject{
    Username: "",           // 空
    Groups:   []string{},   // 空
    ClientID: "my-app",     // 非空
    IP:       net.IP{...},
}
```

`IsAnonymous()` = `Username == "" && len(Groups) == 0` = **true**

### 3.2 三层匿名放宽的叠加效应

Client Credentials 请求在策略评估过程中会经历**三层匿名放宽**：

| 层级 | 位置 | 放宽逻辑 | Client Credentials 结果 |
|------|------|---------|------------------------|
| **第一层** | 域名匹配 | `IsAnonymous()=true` → 子域非空即匹配 | `{user}.example.com` 对任何子域名匹配 |
| **第二层** | 资源匹配 | `IsAnonymous()=true` → 正则匹配即返回 true | `(?P<User>...)` 路径对任何匹配路径通过 |
| **第三层** | 主体匹配 | `IsAnonymous()=true` → 直接返回 true | `oauth2:client:xxx` 规则被完全跳过 |

**叠加后果**：一个 Client Credentials 请求可以同时绕过域名身份约束、路径身份约束和显式主体约束。

### 3.3 具体场景分析

**场景 1：纯 {user} 域名 + 无显式 subject**

```yaml
- domain: "{user}.example.com"
  policy: one_factor
```

| 请求者 | 域名 | Domain匹配 | Subject匹配 | 结果 |
|--------|------|-----------|------------|------|
| 匿名用户 | john.example.com | ✅ (匿名放宽) | ✅ (无subject) | OneFactor |
| ClientID=my-app | john.example.com | ✅ (匿名放宽) | ✅ (无subject) | OneFactor |
| John(已认证) | john.example.com | ✅ (Username=john) | ✅ | OneFactor |
| John(已认证) | bob.example.com | ❌ (Username≠bob) | ✅ | 下一规则 |

Client Credentials 请求与真正匿名用户行为完全相同——都能匹配任何 `{user}` 子域名。这在 `one_factor` 策略下不是安全漏洞（需要认证），但在 `bypass` 策略下是危险的。

**场景 2：{user} 域名 + oauth2:client 主体**

```yaml
- domain: "{user}.example.com"
  subject: "oauth2:client:trusted-app"
  policy: bypass
```

| 请求者 | 域名 | Domain匹配 | Subject匹配 | 结果 |
|--------|------|-----------|------------|------|
| ClientID=trusted-app | john.example.com | ✅ (匿名放宽) | ✅ (匿名跳过!) | **Bypass** |
| ClientID=malicious | john.example.com | ✅ (匿名放宽) | ✅ (匿名跳过!) | **Bypass** ← 安全漏洞 |
| John(已认证) | john.example.com | ✅ | ❌ (ClientID≠trusted-app 且无 Username 匹配) | 下一规则 |

**核心问题**：`subject: ["oauth2:client:trusted-app"]` 规则中的 `AccessControlClient.IsMatch()` 因为匿名短路而从未被调用，任何 Client Credentials 请求都能绕过客户端身份检查。

**场景 3：{group} 域名 + 混合主体**

```yaml
- domain: "{group}.example.com"
  subject: ["group:admins", "oauth2:client:admin-bot"]
  policy: one_factor
```

| 请求者 | 域名 | Domain匹配 | Subject匹配 | 结果 |
|--------|------|-----------|------------|------|
| ClientID=admin-bot | admins.example.com | ✅ (匿名放宽) | ✅ (匿名跳过!) | OneFactor |
| ClientID=malicious | admins.example.com | ✅ (匿名放宽) | ✅ (匿名跳过!) | **OneFactor** ← 漏洞 |
| John(已认证, admins组) | admins.example.com | ✅ (Group=admins) | ✅ (group:admins) | OneFactor |
| John(已认证, users组) | admins.example.com | ❌ (Group∉Groups) | - | 下一规则 |

### 3.4 {user} 域名匹配中的特殊边界

`access_control_domain.go:67-72` 中 `{user}` 匹配的匿名分支：

```go
case m.UserWildcard:
    if subject.IsAnonymous() && strings.HasSuffix(domain, m.Name) {
        return len(domain) > len(m.Name)   // 只要子域非空就匹配
    }
```

**ClientID 存在时的异常**：

对于 `Subject{ClientID:"my-app", Username:"", Groups:[]}`，请求 `my-app.example.com`：
- `IsAnonymous()` = true → 进入匿名分支
- `strings.HasSuffix("my-app.example.com", ".example.com")` = true
- `len("my-app.example.com") > len(".example.com")` = true
- 返回 `true` → 域名匹配成功

但即使 ClientID 恰好与子域名一致（如 `my-app.example.com`），这也不是有意匹配，因为匹配逻辑完全没有使用 `ClientID` 字段。如果 ClientID 是 `other-app`，请求 `my-app.example.com` 同样会匹配。

### 3.5 正则域名/资源的 ClientID 交互

`RegexpGroupStringSubjectMatcher.IsMatch()` 的匿名分支：

```go
if subject.IsAnonymous() {
    return true  // 只要正则匹配就通过
}
```

**Client Credentials 请求访问 `(?P<User>...)` 资源**：

```
请求: GET /john/personal
Subject: {ClientID:"my-app", Username:"", Groups:[]}

正则: ^/(?P<User>[a-zA-Z0-9]+)/personal(/|/.*)?$
matches = ["/john/personal", "john"]

IsAnonymous() = true → return true

对比 John(已认证):
matches[r.SubexpNameUser] = "john"
subject.Username = "john"
→ EqualFold("john", "john") = true → 匹配

对比 Bob(已认证):
matches[r.SubexpNameUser] = "john"
subject.Username = "bob"
→ EqualFold("bob", "john") = false → 不匹配
```

Client Credentials 请求能匹配任何 `(?P<User>...)` 路径，即使该路径属于其他用户。

---

## 四、isAuthzResult 中 HasSubjects 标记对 Client Credentials 的影响

### 4.1 HasSubjects 的二次判定逻辑

`handler_authz_util.go:28-43`：

```go
func isAuthzResult(level authentication.Level, required authorization.Level, ruleHasSubject bool) AuthzResult {
    switch {
    case required == authorization.Bypass:
        return AuthzResultAuthorized
    case required == authorization.Denied && (level != authentication.NotAuthenticated || !ruleHasSubject):
        return AuthzResultForbidden
    case required == authorization.OneFactor && level >= authentication.OneFactor,
         required == authorization.TwoFactor && level >= authentication.TwoFactor:
        return AuthzResultAuthorized
    default:
        return AuthzResultUnauthorized
    }
}
```

### 4.2 Client Credentials 在不同策略下的结果

Client Credentials 请求的 `authn.Level = OneFactor`（来自 `handleVerifyGETAuthorizationBearerIntrospection:631`）。

| 策略 | ruleHasSubject | 结果 | 分析 |
|------|---------------|------|------|
| bypass | (无关) | **Authorized** | 直接授权，无需认证 |
| one_factor | true | **Authorized** | Level=OneFactor >= OneFactor |
| one_factor | false | **Authorized** | 同上 |
| two_factor | true/false | **Unauthorized** | Level=OneFactor < TwoFactor |
| denied | true | **Forbidden** | Level=OneFactor ≠ NotAuthenticated |
| denied | false | **Forbidden** | 同上 |

**关键差异**：Client Credentials 请求在 `denied` 策略下总是返回 `Forbidden`（因为 `level != NotAuthenticated`），不会返回 `Unauthorized`。这与真正匿名用户（`level = NotAuthenticated`）在 `denied + ruleHasSubject=true` 时返回 `Unauthorized` 不同。

**但这恰好意味着**：Client Credentials 请求不会因为 `ruleHasSubject` 的存在而获得"登录提示"的"优惠"，它在 denied 策略下直接被 403 拒绝。这算是一个部分安全补偿——虽然匹配阶段绕过了主体约束，但至少 denied 策略能正确拦截。

**问题在于 non-denied 策略**：`one_factor` 和 `bypass` 策略下，Client Credentials 请求因为 `MatchesSubjects` 的匿名跳过而匹配了本不该匹配的规则，直接获得授权。

---

## 五、配置替代方案仍导致语义放宽的场景

### 5.1 场景矩阵

| 配置方式 | HasSubjects | 匿名/CC放宽 | ClientID检查 | 安全风险 |
|---------|-------------|------------|-------------|---------|
| `domain: "{user}.example.com"` + `policy: bypass` | **true** | ✅ 域名+主体 | ❌ 跳过 | **高** |
| `domain: "{user}.example.com"` + `policy: one_factor` | **true** | ✅ 域名+主体 | ❌ 跳过 | 中（需认证） |
| `domain: "*.example.com"` + `subject: ["oauth2:client:app"]` | **true** | ✅ 主体 | ❌ 跳过 | **高** |
| `domain: "*.example.com"` + `policy: one_factor` | **false** | ❌ 无 | N/A | 低 |
| `domain_regex: "^(?P<User>...)\\.com$"` + `policy: bypass` | **true** | ✅ 域名+主体 | ❌ 跳过 | **高** |
| `domain_regex: "^.*\\.example.com$"` + `subject: ["oauth2:client:app"]` | **true** | ✅ 主体 | ❌ 跳过 | **高** |
| `domain_regex: "^.*\\.example.com$"` + `policy: one_factor` | **false** | ❌ 无 | N/A | 低 |
| `resources: ["^/(?P<User>...)/api$"]` + `subject: ["oauth2:client:app"]` | **true** | ✅ 资源+主体 | ❌ 跳过 | **高** |
| `resources: ["^/api/.*$"]` + `subject: ["oauth2:client:app"]` | **true** | ✅ 主体 | ❌ 跳过 | **高** |

### 5.2 语义放宽的根本原因分类

**第一类：IsAnonymous() 不检查 ClientID**

影响范围：所有使用 `subject: ["oauth2:client:xxx"]` 的规则。

```
根因: Subject.IsAnonymous() = Username=="" && len(Groups)==0
      → Client Credentials: Username="" Groups=[] → IsAnonymous()=true
      → MatchesSubjects() 直接返回 true
      → AccessControlClient.IsMatch() 从未被调用

放宽表现: 任何有效的 Client Credentials Token 都能匹配
          包含 oauth2:client 主体约束的规则
```

**此放宽不可通过配置规避**。只要规则包含 `subject: ["oauth2:client:xxx"]`，Client Credentials 请求就会绕过检查。

**第二类：域名/资源匹配器的匿名放宽**

影响范围：所有使用 `{user}`、`{group}`、`(?P<User>...)`、`(?P<Group>...)` 的规则。

```
根因: AccessControlDomainMatcher.IsMatch() 对匿名用户:
      UserWildcard: 只检查子域非空，不检查 Username
      GroupWildcard: 只检查子域非空，不检查 Groups

      RegexpGroupStringSubjectMatcher.IsMatch() 对匿名用户:
      只要正则匹配就返回 true，跳过 User/Group 验证

      Client Credentials: IsAnonymous()=true → 触发所有匿名放宽
```

**此放宽是匿名放宽设计的一部分**，对真正匿名用户是有意为之（让用户有机会登录），但对 Client Credentials 是误伤。

**第三类：HasSubjects 标记的来源不一致**

```
根因: HasSubjects 标记来自三个来源:
      1. 显式 subject 规则
      2. 域名 {user}/{group} 或 (?P<User/Group>...)
      3. 资源 (?P<User/Group>...)

      但 isAuthzResult 只区分 ruleHasSubject=true/false,
      不区分是哪种来源。
```

这意味着一条只含 `{user}.example.com` 域名（无显式 subject）的规则，和一条含 `subject: ["oauth2:client:xxx"]` 的规则，在 `isAuthzResult` 中获得相同的 `ruleHasSubject=true` 处理。虽然两者都需要身份信息，但所需身份的**类型**不同——前者需要 Username/Groups，后者需要 ClientID——而 `IsAnonymous()` 只检查前两者。

### 5.3 不可规避的配置模式

以下配置模式**无论如何替代**，在当前代码下仍会导致语义放宽：

**模式 A：oauth2:client 主体 + 任何域名/资源**

```yaml
- domain: "api.example.com"          # 即使是精确域名
  subject: "oauth2:client:trusted"    # 只要有 oauth2:client 主体
  policy: bypass                      # → 任何 CC 请求都能匹配
```

**替代尝试 1**：改用 `one_factor` 策略
```yaml
- domain: "api.example.com"
  subject: "oauth2:client:trusted"
  policy: one_factor
```
结果：CC 请求仍匹配此规则，因 `Level=OneFactor >= OneFactor` → **Authorized**。语义放宽仍在。

**替代尝试 2**：增加 network 限制
```yaml
- domain: "api.example.com"
  subject: "oauth2:client:trusted"
  networks: "10.0.0.0/8"
  policy: bypass
```
结果：CC 请求仍匹配此规则（网络条件与主体条件是 AND 关系，但主体条件被匿名跳过）。只要 IP 在范围内，语义放宽仍在。

**模式 B：{user}/{group} 域名 + bypass 策略**

```yaml
- domain: "{user}.example.com"
  policy: bypass
```

结果：CC 请求匹配任何 `{user}.example.com` 子域名，直接授权。

**替代尝试**：改用 `one_factor`
```yaml
- domain: "{user}.example.com"
  policy: one_factor
```
结果：CC 请求匹配规则后，`Level=OneFactor >= OneFactor` → **Authorized**。语义放宽仍在。

**模式 C：(?P<User>) 资源 + 任何策略**

```yaml
- domain: "api.example.com"
  resources: ["^/(?P<User>[a-z]+)/data$"]
  policy: one_factor
```

结果：CC 请求匹配任何 `/{username}/data` 路径，且 `Level=OneFactor` → **Authorized**。

### 5.4 唯一能规避的配置替代

**使用 `two_factor` 策略**：

```yaml
- domain: "{user}.example.com"
  subject: "oauth2:client:trusted"
  policy: two_factor
```

结果：CC 请求 `Level=OneFactor < TwoFactor` → **Unauthorized**。

这是唯一能规避的模式，因为 Client Credentials 的认证级别固定为 `OneFactor`，永远无法达到 `TwoFactor`。但这不是"正确匹配"，而是"碰巧拒绝"——如果未来 Client Credentials 支持 MFA，此规避将失效。

**使用 `denied` 策略**：

```yaml
- domain: "{user}.example.com"
  subject: "oauth2:client:trusted"
  policy: deny
```

结果：CC 请求 → `Forbidden` (403)。同样不是"正确匹配"，而是"全部拒绝"。

---

## 六、Bypass 策略验证器的遗漏

### 6.1 当前验证逻辑

`access_control.go:64-75`：

```go
func validateBypass(rulePosition int, rule schema.AccessControlRule, validator *schema.StructValidator) {
    if len(rule.Subjects) != 0 {
        validator.Push(fmt.Errorf(...))  // bypass + 显式 subject → 报错
    }

    for _, pattern := range rule.DomainsRegex {
        if utils.IsStringSliceContainsAny(authorization.IdentitySubexpNames, pattern.SubexpNames()) {
            validator.Push(fmt.Errorf(...))  // bypass + (?P<User/Group>) 正则域名 → 报错
            return
        }
    }
}
```

### 6.2 遗漏的检查项

| 组合 | 已验证？ | 说明 |
|------|---------|------|
| bypass + `subject: ["user:john"]` | ✅ 已禁止 | `len(rule.Subjects) != 0` |
| bypass + `subject: ["oauth2:client:app"]` | ✅ 已禁止 | 同上 |
| bypass + `domain_regex: "^(?P<User>...)\\.com$"` | ✅ 已禁止 | 正则命名组检查 |
| bypass + `domain: "{user}.example.com"` | ❌ **未禁止** | 简单域名 `{user}` 不在检查范围 |
| bypass + `resources: ["^/(?P<User>...)/api$"]` | ❌ **未禁止** | 资源正则命名组不在检查范围 |

**遗漏 1**：`bypass + domain: "{user}.example.com"` 未被验证器拒绝。这会导致匿名用户（和 Client Credentials 请求）在 `{user}` 域名下直接获得 bypass 授权。

**遗漏 2**：`bypass + resources: ["^/(?P<User>...)/api$"]` 未被验证器拒绝。`{user}` 路径下的 bypass 同样存在语义放宽。

### 6.3 遗漏的安全影响

```yaml
# 此配置会通过验证，但语义上等同于 bypass + subject
- domain: "{user}.example.com"
  policy: bypass
```

匿名用户访问 `john.example.com` → 域名匹配（匿名放宽）→ 无 subject 检查 → `policy=bypass` → **直接授权，无需认证**。

对比验证器已禁止的等价配置：
```yaml
# 此配置会被验证器拒绝
- domain_regex: "^(?P<User>[a-z]+)\\.example\\.com$"
  policy: bypass
```

两者语义完全相同，但一个通过验证，一个被拒绝。

---

## 七、完整交互界限图

```
                    ┌──────────────────────────────┐
                    │   Subject.IsAnonymous()       │
                    │   Username=="" && len(Groups)==0 │
                    │   ★ 不检查 ClientID           │
                    └──────────┬───────────────────┘
                               │ true
            ┌──────────────────┼──────────────────────┐
            │                  │                       │
   ┌────────▼────────┐  ┌─────▼──────┐  ┌─────────────▼──────────────┐
   │ 域名匹配放宽     │  │ 资源匹配放宽│  │ 主体匹配放宽                │
   │                 │  │            │  │                             │
   │ {user} 域名:    │  │ (?P<User>) │  │ MatchesSubjects()           │
   │ 子域非空即匹配   │  │ 正则匹配    │  │ 直接返回 true               │
   │ 不查Username    │  │ 即返回true  │  │ 跳过所有SubjectMatcher      │
   │                 │  │ 不查User/   │  │                             │
   │ {group} 域名:   │  │ Group       │  │ AccessControlUser: 跳过     │
   │ 子域非空即匹配   │  │            │  │ AccessControlGroup: 跳过    │
   │ 不查Groups      │  │            │  │ AccessControlClient: 跳过   │
   └────────┬────────┘  └─────┬──────┘  └─────────────┬──────────────┘
            │                  │                       │
            └──────────────────┼───────────────────────┘
                               │
                    ┌──────────▼───────────────────┐
                    │  规则命中 → HasSubjects=true   │
                    │  (来自域名/资源/主体任一来源)   │
                    └──────────┬───────────────────┘
                               │
                    ┌──────────▼───────────────────┐
                    │  isAuthzResult()              │
                    │                               │
                    │  真正匿名(NotAuthenticated):  │
                    │   denied+HasSubjects → 302    │
                    │   其他策略 → 按策略判定        │
                    │                               │
                    │  Client Credentials(OneFactor):│
                    │   denied → 403 (不管HasSubjects)│
                    │   one_factor → Authorized ★    │
                    │   bypass → Authorized ★        │
                    └──────────────────────────────┘
```

---

## 八、总结：语义放宽的完整清单

### 8.1 IsAnonymous() Bug 导致的放宽（影响 oauth2:client 规则）

| 配置 | 预期行为 | 实际行为 | 根因 |
|------|---------|----------|------|
| `subject: ["oauth2:client:app"]` + bypass | 仅 app 客户端 bypass | **所有** CC 客户端 bypass | IsAnonymous()不检查ClientID |
| `subject: ["oauth2:client:app"]` + one_factor | 仅 app 客户端授权 | **所有** CC 客户端授权 | 同上 |
| `subject: ["oauth2:client:app"]` + two_factor | 仅 app 客户端需2FA | 所有 CC 客户端 Unauthorized | 同上（碰巧安全） |
| `subject: ["oauth2:client:app"]` + deny | 仅 app 客户端被拒 | 所有 CC 客户端 403 | 同上（碰巧安全） |

### 8.2 动态匹配匿名放宽导致的放宽（影响 {user}/{group}/(?P<User/Group>)）

| 配置 | 预期行为 | 实际行为 | 根因 |
|------|---------|----------|------|
| `{user}.example.com` + bypass | 仅对应用子域名用户 bypass | **匿名/CC** 直接 bypass | 域名匿名放宽 + bypass验证遗漏 |
| `{user}.example.com` + one_factor | 仅对应用子域名用户需1FA | **匿名/CC** 匹配后授权 | 域名匿名放宽 |
| `(?P<User>) 资源` + bypass | 仅对应用路径用户 bypass | **匿名/CC** 直接 bypass | 资源匿名放宽 + bypass验证遗漏 |
| `(?P<Group>) 域名` + one_factor | 仅对应组子域名需1FA | **匿名/CC** 匹配后授权 | 域名匿名放宽 |

### 8.3 验证器遗漏导致的放宽

| 遗漏配置 | 应有行为 | 实际行为 | 根因 |
|---------|---------|----------|------|
| bypass + `{user}` 域名 | 验证报错 | **通过验证** | validateBypass 不检查简单域名 |
| bypass + `(?P<User>)` 资源 | 验证报错 | **通过验证** | validateBypass 不检查资源正则 |
