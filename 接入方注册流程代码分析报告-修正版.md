# Authelia 接入方注册流程代码分析报告（修正版）

## 分析修正说明

本报告基于对代码库的全面重新核查，纠正了之前分析中的以下不准确之处：

1. **`RotatedClientSecrets` 配置入口缺失**：`RegisteredClient` 结构体中存在该字段，但配置 schema 中无对应配置项
2. **回调地址验证触发时机**：区分了配置加载时验证和运行时验证两个不同阶段
3. **动态注册端点**：明确确认无动态注册端点，`RegistrationEndpoint` 字段存在但未启用
4. **文件监视器范围**：仅用于用户数据库文件，不涉及 OIDC 客户端配置
5. **运行时回调验证**：由 oauthelia2 库内部完成，Authelia 主要提供配置验证

---

## 1. 客户端元数据校验

### 1.1 校验执行时机与调用链路

客户端元数据校验**仅在服务启动时的配置加载阶段执行**，运行时无重复校验。

**完整调用链路**：
```
main 启动
  → LoadConfiguration()
    → ValidateIdentityProviders() [identity_providers.go:22]
      → validateOIDC() [identity_providers.go:26]
        → validateOIDCClients() [identity_providers.go:614]
          → 循环每个客户端:
            - client_id 格式/唯一性校验 [identity_providers.go:628-647]
            - validateOIDCClient() [identity_providers.go:668]
```

### 1.2 核心校验函数 `validateOIDCClients`

**代码位置**：`internal/configuration/validator/identity_providers.go:614-665`

```go
func validateOIDCClients(ctx *ValidateCtx, config *schema.IdentityProvidersOpenIDConnect, validator *schema.StructValidator) {
    for c, client := range config.Clients {
        n := len(client.ID)
        switch {
        case n == 0:
            blankClientIDs = append(blankClientIDs, "#"+strconv.Itoa(c+1))
        case n > 100:
            validator.Push(fmt.Errorf(errFmtOIDCClientIDTooLong, client.ID, n))
        case !reRFC3986Unreserved.MatchString(client.ID):
            validator.Push(fmt.Errorf(errFmtOIDCClientIDInvalidCharacters, client.ID))
        default:
            if client.Name == "" {
                config.Clients[c].Name = client.ID  // 默认使用 client_id
            }
            // 重复 ID 检查
            if utils.IsStringInSlice(client.ID, clientIDs) {
                duplicateClientIDs = append(duplicateClientIDs, client.ID)
            } else {
                clientIDs = append(clientIDs, client.ID)
            }
        }
        validateOIDCClient(ctx, c, config, validator, errDeprecatedFunc)
    }
    // 汇总错误：空 ID、重复 ID 等
}
```

**校验要点**：
- **client_id 长度**：1-100 字符
- **字符集限制**：仅允许 RFC3986 非保留字符（`ALPHA / DIGIT / "-" / "." / "_" / "~"`）
- **唯一性检查**：全局客户端 ID 去重
- **默认值处理**：`client_name` 为空时自动使用 `client_id`

### 1.3 单个客户端完整校验 `validateOIDCClient`

**代码位置**：`internal/configuration/validator/identity_providers.go:668-775`

该函数执行 20+ 项校验，核心校验项如下表：

| 校验项 | 校验函数 | 代码位置 | 校验规则 |
|-------|---------|---------|---------|
| 授权策略 | 内联 | 671-684 | 必须是已配置的策略，`client_credentials` 模式强制为 `one_factor` |
| 生命周期 | 内联 | 686-695 | 必须引用已配置的自定义生命周期 |
| PKCE 方法 | 内联 | 697-702 | 仅允许 `""`/`plain`/`S256` |
| Audience 模式 | 内联 | 704-711 | 仅允许 `explicit`/`implicit` |
| Consent 模式 | `validateOIDCClientConsentMode` | 937-958 | `auto`/`explicit`/`implicit`/`pre-configured` |
| Scopes | `validateOIDCClientScopes` | 961-1025 | 必须在支持的 scope 列表中，检查重复项 |
| Response Types | `validateOIDCClientResponseTypes` | 1109-1128 | 必须在支持的响应类型列表中 |
| Response Modes | `validateOIDCClientResponseModes` | 1131-1165 | 必须在支持的响应模式列表中 |
| Grant Types | `validateOIDCClientGrantTypes` | 1167-1189 | 必须在支持的授权类型列表中 |
| **Redirect URIs** | `validateOIDCClientRedirectURIs` | 1253-1287 | 见 2.1 节 |
| Request URIs | `validateOIDCClientRequestURIs` | 1289-1314 | 必须是绝对 HTTPS URL |
| 签名算法 | `validateOIDDClientSigningAlgs` | - | 必须在支持的签名算法列表中 |
| 加密算法 | `validateOIDDClientEncryptionAlgs` | - | 必须在支持的加密算法列表中 |
| **Sector Identifier** | `validateOIDCClientSectorIdentifier` | 887-935 | 见 2.2 节 |
| 公钥配置 | `validateOIDCClientPublicKeys` | 777-788 | `jwks_uri` 和 `jwks` 不能同时配置 |
| 端点认证方法 | `validateOIDCClientEndpointAuth` | 1316-1380 | 4 个端点（token/introspection/revocation/PAR）独立校验 |
| Secret 要求 | 内联 | 768-774 | 机密客户端必须配置 secret，公共客户端不得配置 |

### 1.4 端点认证方法校验

**代码位置**：`internal/configuration/validator/identity_providers.go:1316-1380`

每个端点独立校验 `auth_method` 和 `auth_signing_alg`，返回标志位：
- `confidential`: 机密客户端但未配置 secret → 错误
- `public`: 公共客户端但配置了 secret → 错误

支持的认证方法：
- `none`: 无认证（公共客户端）
- `client_secret_basic`: HTTP Basic 认证
- `client_secret_post`: Form 参数认证
- `client_secret_jwt`: JWT 认证（使用客户端 secret 签名）
- `private_key_jwt`: JWT 认证（使用客户端私钥签名）

---

## 2. 回调地址严格匹配机制

### 2.1 基本格式验证（配置加载时）

**代码位置**：`internal/configuration/validator/identity_providers.go:1253-1287`

```go
func validateOIDCClientRedirectURIs(c int, config *schema.IdentityProvidersOpenIDConnect, validator *schema.StructValidator, errDeprecatedFunc func()) {
    for _, redirectURI := range config.Clients[c].RedirectURIs {
        // 公共客户端例外：允许 urn:ietf:wg:oauth:2.0:oob
        if redirectURI == oidc.RedirectURISpecialOAuth2InstalledApp {
            if config.Clients[c].Public {
                continue
            }
            validator.Push(fmt.Errorf(errFmtOIDCClientRedirectURISpecial, config.Clients[c].ID, redirectURI))
            continue
        }
        // 必须可解析
        if parsedRedirectURI, err = url.Parse(redirectURI); err != nil {
            validator.Push(fmt.Errorf(errFmtOIDCClientRedirectURICantBeParsed, config.Clients[c].ID, redirectURI, err))
            continue
        }
        // 必须是绝对 URL（有 scheme）
        if !parsedRedirectURI.IsAbs() {
            validator.Push(fmt.Errorf(errFmtOIDCClientRedirectURIAbsolute, config.Clients[c].ID, redirectURI))
        }
    }
}
```

### 2.2 Sector Identifier 严格匹配（配置加载时）

**代码位置**：`internal/configuration/validator/identity_providers.go:887-935` → 调用 `internal/oidc/util.go:463-490`

```go
func validateOIDCClientSectorIdentifier(ctx *ValidateCtx, c int, config *schema.IdentityProvidersOpenIDConnect, validator *schema.StructValidator, errDeprecatedFunc func()) {
    if config.Clients[c].SectorIdentifierURI == nil {
        return
    }
    // ... 验证 SectorIdentifierURI 本身格式（HTTPS、无 fragment、无 userinfo）
    if valid {
        // 关键：调用严格匹配验证
        if err := oidc.ValidateSectorIdentifierURI(ctx, ctx.cacheSectorIdentifierURIs, config.Clients[c].SectorIdentifierURI, config.Clients[c].RedirectURIs); err != nil {
            validator.Push(fmt.Errorf(errFmtOIDCClientInvalidSectorIdentifierRedirect, config.Clients[c].ID, config.Clients[c].SectorIdentifierURI.String(), err))
        }
    }
}
```

**`ValidateSectorIdentifierURI` 核心逻辑**：
```go
func ValidateSectorIdentifierURI(ctx ClientContext, cache map[string][]string, sectorURI *url.URL, redirectURIs []string) (err error) {
    // 1. 拉取 Sector Identifier 文档（JSON 数组格式）
    sectorRedirectURIs, err = getSectorIdentifierURICache(ctx, cache, sectorURI)
    
    // 2. 对每个回调地址使用 BestPracticeURIComparisonStrategy 严格匹配
    for _, rawRedirectURI := range redirectURIs {
        if _, match := oauthelia2.IsMatchingRedirectURI(
            rawRedirectURI, 
            sectorRedirectURIs, 
            &oauthelia2.BestPracticeURIComparisonStrategy{},
        ); match {
            continue
        }
        invalidRedirectURIs = append(invalidRedirectURIs, rawRedirectURI)
    }
    // 3. 返回不匹配错误
    if len(invalidRedirectURIs) > 0 {
        return fmt.Errorf("error checking redirect_uri '%s' against '%s'", ...)
    }
    return nil
}
```

**BestPracticeURIComparisonStrategy 匹配规则**（遵循 OAuth 2.0 Security BCP）：
- ✅ Scheme 必须完全一致（`https` ≠ `http`）
- ✅ Host 必须完全一致（`app.example.com` ≠ `example.com`）
- ✅ Port 必须完全一致（默认端口省略视为匹配）
- ✅ Path 必须完全一致（区分大小写，`/callback` ≠ `/Callback`）
- ✅ Query 参数必须完全一致（顺序不敏感）
- ❌ 不支持通配符（`*.example.com` 不被允许）
- ⚠️ Fragment 在比较时被忽略（OAuth 规范要求）

### 2.3 运行时回调地址验证（oauthelia2 库内部）

**关键发现**：运行时的回调地址验证**完全由 oauthelia2 库内部完成**，Authelia 代码中不直接处理。

**触发链路**：
```
OAuth2AuthorizationGET [handler_oauth2_authorization.go:22]
  → ctx.Providers.OpenIDConnect.NewAuthorizeRequest(ctx, r)  [oauthelia2 库内部]
    → 验证 redirect_uri 是否在 client.GetRedirectURIs() 列表中
    → 调用 GetRedirectSecureChecker() 进行安全性检查
```

**Authelia 提供的安全检查器**：
```go
// internal/oidc/config.go:499-502
func (c *Config) GetRedirectSecureChecker(ctx context.Context) func(context.Context, *url.URL) (secure bool) {
    return oauthelia2.IsRedirectURISecure  // 检查是否为 https 等安全方案
}
```

**运行时验证的完整调用链证据**：
- Authelia 仅实现 `GetRedirectURIs()` 接口返回配置的回调列表
- `GetRedirectSecureChecker()` 提供安全规则
- 实际匹配逻辑在 oauthelia2 库的 `authorize_handler.go` 中

### 2.4 回调地址验证时机总结

| 验证阶段 | 执行时机 | 验证内容 | 代码位置 |
|---------|---------|---------|---------|
| 配置加载验证 | 服务启动时 | 格式合法性 + Sector Identifier 严格匹配 | `validator/identity_providers.go:1253-1287, 887-935` |
| 运行时验证 | 每次授权请求时 | redirect_uri 是否在白名单中 + 安全性 | oauthelia2 库内部，Authelia 提供数据 |

---

## 3. 静态配置与动态注册边界

### 3.1 已注册的 OIDC 端点清单

**代码位置**：`internal/server/handlers.go:392-496`

```go
func RegisterOpenIDConnectRoutes(r *router.Router, config *schema.Configuration, providers middlewares.Providers) {
    // 发现端点
    r.GET(oidc.EndpointPathWellKnownOAuthAuthorizationServer, ...)
    r.GET(oidc.EndpointPathWellKnownOpenIDConfiguration, ...)
    r.GET(oidc.EndpointPathJWKs, ...)
    
    // 授权相关端点
    r.GET(oidc.EndpointPathAuthorization, ...)  // /api/oidc/authorization
    r.POST(oidc.EndpointPathAuthorization, ...)
    r.POST(oidc.EndpointPathPushedAuthorizationRequest, ...)
    r.POST(oidc.EndpointPathDeviceAuthorization, ...)
    
    // Token 相关端点
    r.POST(oidc.EndpointPathToken, ...)          // /api/oidc/token
    r.POST(oidc.EndpointPathIntrospection, ...)  // /api/oidc/introspection
    r.POST(oidc.EndpointPathRevocation, ...)     // /api/oidc/revocation
    
    // 用户信息端点
    r.GET(oidc.EndpointPathUserinfo, ...)        // /api/oidc/userinfo
    r.POST(oidc.EndpointPathUserinfo, ...)
    
    // Consent 端点
    r.GET(oidc.EndpointPathConsent, ...)         // /api/oidc/consent
    r.POST(oidc.EndpointPathConsent, ...)
    
    // ⚠️  注意：没有任何 register 相关端点
}
```

**端点路径常量**（`internal/oidc/const.go:292-306`）：
```go
EndpointPathWellKnownOpenIDConfiguration      = "/.well-known/openid-configuration"
EndpointPathWellKnownOAuthAuthorizationServer = "/.well-known/oauth-authorization-server"
EndpointPathJWKs                              = "/jwks.json"
EndpointPathRoot                              = "/api/oidc"
EndpointPathAuthorization                     = "/api/oidc/authorization"
EndpointPathToken                             = "/api/oidc/token"
EndpointPathUserinfo                          = "/api/oidc/userinfo"
EndpointPathIntrospection                     = "/api/oidc/introspection"
EndpointPathRevocation                        = "/api/oidc/revocation"
EndpointPathDeviceAuthorization               = "/api/oidc/device-authorization"
EndpointPathPushedAuthorizationRequest        = "/api/oidc/pushed-authorization-request"
```

**结论**：共 9 个端点，**无任何动态注册端点**（如 `/connect/register` 或 `/api/oidc/register`）。

### 3.2 发现文档中的注册端点

**代码位置**：`internal/oidc/provider.go:38-70`

```go
func (p *OpenIDConnectProvider) GetOAuth2WellKnownConfiguration(issuer string) OAuth2WellKnownConfiguration {
    options := p.discovery.OAuth2WellKnownConfiguration.Copy()
    options.Issuer = issuer
    options.JWKSURI = fmt.Sprintf("%s%s", issuer, EndpointPathJWKs)
    options.AuthorizationEndpoint = fmt.Sprintf("%s%s", issuer, EndpointPathAuthorization)
    options.DeviceAuthorizationEndpoint = fmt.Sprintf("%s%s", issuer, EndpointPathDeviceAuthorization)
    options.PushedAuthorizationRequestEndpoint = fmt.Sprintf("%s%s", issuer, EndpointPathPushedAuthorizationRequest)
    options.TokenEndpoint = fmt.Sprintf("%s%s", issuer, EndpointPathToken)
    options.IntrospectionEndpoint = fmt.Sprintf("%s%s", issuer, EndpointPathIntrospection)
    options.RevocationEndpoint = fmt.Sprintf("%s%s", issuer, EndpointPathRevocation)
    // ⚠️  注意：没有设置 RegistrationEndpoint
    return options
}
```

**结构体定义中存在但未使用**：
```go
// internal/oidc/types.go:483
RegistrationEndpoint string `json:"registration_endpoint,omitempty"`
```

**关键发现**：`RegistrationEndpoint` 字段在结构体中定义，但在生成发现文档的代码中**从未被赋值**，因此对外不会暴露。

### 3.3 动态注册支持状态确认

| 检查项 | 状态 | 证据 |
|-------|------|------|
| 注册端点路由 | ❌ 不存在 | `RegisterOpenIDConnectRoutes` 中无相关路由 |
| 注册处理器 | ❌ 不存在 | `handlers` 包中无相关处理函数 |
| 发现文档暴露 | ❌ 未暴露 | `GetOAuth2WellKnownConfiguration` 未设置该字段 |
| 动态客户端存储 | ❌ 不支持 | 仅 `MemoryClientStore` 从配置加载 |
| RFC7591 支持 | ❌ 不支持 | 无相关代码实现 |

### 3.4 静态配置存储机制

**代码位置**：`internal/oidc/store.go:27-59`

```go
func NewMemoryClientStore(config *schema.Configuration) (store *MemoryClientStore) {
    store = &MemoryClientStore{
        clients: map[string]Client{},
    }
    // 仅在初始化时从配置加载，运行时不更新
    for _, client := range config.IdentityProviders.OIDC.Clients {
        store.clients[client.ID] = NewClient(client, config.IdentityProviders.OIDC, policies)
    }
    return store
}

// 运行时查询，只读
func (s *MemoryClientStore) GetRegisteredClient(_ context.Context, id string) (client Client, err error) {
    client, ok := s.clients[id]
    if !ok {
        return nil, oauthelia2.ErrInvalidClient.WithDebugf("Client with id '%s' does not appear to be a registered client.", id)
    }
    return client, nil
}
```

**存储特性**：
- 内存存储，服务重启后重新加载
- 运行时只读，无新增/修改/删除接口
- 所有客户端必须在配置文件中预先定义

---

## 4. 配置变更后已发凭据兼容处理机制

### 4.1 密钥轮换机制的代码实现与配置边界

#### 4.1.1 代码层面的支持

**`RegisteredClient` 结构体定义**（`internal/oidc/types.go:62-63`）：
```go
type RegisteredClient struct {
    ClientSecret         *ClientSecretDigest    // 当前密钥
    RotatedClientSecrets []*ClientSecretDigest // 已轮换的历史密钥
    // ...
}
```

**接口实现**（`internal/oidc/types.go:1123`）：
```go
_ oauthelia2.RotatedClientSecretsClient = (*RegisteredClient)(nil)
```

**GetRotatedClientSecrets 实现**（`internal/oidc/client.go:159-168`）：
```go
func (c *RegisteredClient) GetRotatedClientSecrets() (secrets []oauthelia2.ClientSecret) {
    secrets = make([]oauthelia2.ClientSecret, len(c.RotatedClientSecrets))
    for i, secret := range c.RotatedClientSecrets {
        secrets[i] = secret
    }
    return secrets
}
```

#### 4.1.2 配置入口缺失（关键发现！）

**配置 schema 定义**（`internal/configuration/schema/identity_providers.go:157-239`）：
```go
type IdentityProvidersOpenIDConnectClient struct {
    ID                  string          `koanf:"client_id" ...`
    Name                string          `koanf:"client_name" ...`
    Secret              *PasswordDigest `koanf:"client_secret" ...`
    // ⚠️  注意：没有 rotated_client_secrets 字段！
    SectorIdentifierURI *url.URL        `koanf:"sector_identifier_uri" ...`
    // ... 其他 50+ 字段
}
```

**`NewClient` 初始化**（`internal/oidc/client.go:20-109`）：
```go
func NewClient(config schema.IdentityProvidersOpenIDConnectClient, ...) (client Client) {
    registered := &RegisteredClient{
        ID:                  config.ID,
        Name:                config.Name,
        ClientSecret:        &ClientSecretDigest{PasswordDigest: config.Secret},
        // ⚠️  注意：RotatedClientSecrets 从未被赋值！
        SectorIdentifierURI: config.SectorIdentifierURI,
        // ... 其他字段赋值
    }
    // ...
    return registered
}
```

**关键结论**：
- ✅ 代码层面：`RotatedClientSecrets` 字段、接口、getter 方法完整实现
- ❌ 配置层面：schema 中无对应字段，`NewClient` 中无赋值逻辑
- ❌ 实际效果：该字段始终为 `nil`，密钥轮换功能**当前无法使用**

### 4.2 全局密钥轮换

**全局密钥配置**（`internal/oidc/config.go:80-86`）：
```go
type Config struct {
    GlobalSecret         []byte     // 当前全局密钥
    RotatedGlobalSecrets [][]byte   // 已轮换的历史全局密钥
}

// internal/oidc/config.go:661-664
func (c *Config) GetRotatedGlobalSecrets(ctx context.Context) (secrets [][]byte, err error) {
    return c.RotatedGlobalSecrets, nil
}
```

**同样的问题**：配置 schema 中无 `rotated_global_secrets` 或类似字段，该字段也始终为空。

### 4.3 配置重载机制的实际范围

**文件监视器服务**（`internal/service/file_watcher.go:15-32`）：
```go
func ProvisionUsersFileWatcher(ctx Context) (service Provider, err error) {
    config := ctx.GetConfiguration()
    providers := ctx.GetProviders()
    
    // ⚠️  仅用于用户认证后端文件！
    if config.AuthenticationBackend.File != nil && config.AuthenticationBackend.File.Watch {
        provider, ok := providers.UserProvider.(*authentication.FileUserProvider)
        // ...
        service, err = NewFileWatcher("users", config.AuthenticationBackend.File.Path, provider, ...)
    }
    return service, nil
}
```

**关键发现**：文件监视器**仅用于 `users.yml` 用户数据库文件**的热重载，不涉及 OIDC 客户端配置。OIDC 客户端配置变更必须重启服务。

### 4.4 已发凭据的生命周期管理

#### 4.4.1 Token 存储机制

**代码位置**：`internal/oidc/store.go:420-428`
```go
func (s *Store) saveSession(ctx context.Context, sessionType storage.OAuth2SessionType, signature string, r oauthelia2.Requester) (err error) {
    var session *model.OAuth2Session
    if session, err = model.NewOAuth2SessionFromRequest(signature, r); err != nil {
        return err
    }
    return s.provider.SaveOAuth2Session(ctx, sessionType, *session)
}
```

**存储位置**：数据库（SQLite/MySQL/PostgreSQL），不受服务重启影响。

#### 4.4.2 Token 生命周期（默认值）

| Token 类型 | 默认有效期 | 配置位置 | 过期后行为 |
|-----------|-----------|---------|-----------|
| Access Token | 60 分钟 | `lifespans.access_token` | 自动失效，需重新获取 |
| Refresh Token | 90 分钟 | `lifespans.refresh_token` | 可用于刷新 Access Token |
| ID Token | 60 分钟 | `lifespans.id_token` | 一次性使用，过期后需重新认证 |
| Authorize Code | 1 分钟 | `lifespans.authorize_code` | 一次性使用，过期作废 |

**代码证据**（`internal/configuration/schema/identity_providers.go:242-253`）：
```go
var DefaultOpenIDConnectConfiguration = IdentityProvidersOpenIDConnect{
    Lifespans: IdentityProvidersOpenIDConnectLifespans{
        IdentityProvidersOpenIDConnectLifespanToken: IdentityProvidersOpenIDConnectLifespanToken{
            AccessToken:   time.Hour,
            AuthorizeCode: time.Minute,
            IDToken:       time.Hour,
            RefreshToken:  time.Minute * 90,
        },
        DeviceCode: time.Minute * 10,
    },
}
```

#### 4.4.3 配置变更对已发 Token 的影响

| 配置项变更 | 对已发 Token 的影响 | 兼容机制 | 代码证据 |
|-----------|----------------|---------|---------|
| **client_secret** | 新请求使用旧 secret 会失败 | ❌ 无兼容（RotatedClientSecrets 未启用） | schema 中无 rotated_client_secrets 字段 |
| **redirect_uris** | 旧回调地址的刷新请求被拒绝 | ❌ 无兼容，需用户重新授权 | oauthelia2 库运行时验证 |
| **scopes** | 已颁发 Token 的 scope 不变，刷新时使用新配置 | ⚠️ 部分兼容（旧 scope 保留） | Token 存储中包含 granted_scopes |
| **grant_types** | 已颁发 Token 仍可使用，新授权受限 | ⚠️ 部分兼容 | 运行时校验 grant_type |
| **signing_alg** | 已颁发 Token 仍可验证（使用对应 key id） | ✅ 完全兼容 | JWKS 中包含所有密钥 |
| **jwks** | 旧密钥保留在 JWKS 中，可验证旧 Token | ✅ 完全兼容 | JWKS 密钥列表管理 |
| **lifespans** | 已发 Token 按原生命周期，新 Token 用新配置 | ⚠️ 部分兼容 | Token 过期时间在签发时确定 |

### 4.5 令牌撤销机制

**主动撤销端点**：`/api/oidc/revocation`（RFC7009）

**代码位置**：`internal/handlers/handler_oauth2_revocation.go:17`
```go
func OAuth2RevocationPOST(ctx *middlewares.AutheliaCtx, rw http.ResponseWriter, req *http.Request) {
    // 调用 oauthelia2 库的 NewRevocationRequest
    // 最终调用 s.provider.RevokeOAuth2Session 标记为 revoked
}
```

**数据库中的撤销标记**（`internal/model/oidc.go:92-102`）：
```go
type OAuth2Session struct {
    // ...
    Active    bool   // 是否已撤销
    Revoked   bool   // 是否已撤销
    // ...
}
```

**关键发现**：配置变更**不会自动触发**任何已发 Token 的撤销，只有：
1. 用户通过撤销端点主动撤销
2. Token 自然过期
3. 刷新 Token 时检测到授权已撤销

---

## 5. 与之前分析的差异修正

| 分析项 | 之前结论 | 修正后结论 | 证据 |
|-------|---------|-----------|------|
| RotatedClientSecrets | 可通过配置实现密钥轮换 | 代码实现完整但**配置入口缺失**，实际不可用 | schema 中无 `rotated_client_secrets` 字段，NewClient 无赋值 |
| Sector Identifier 验证时机 | 运行时请求时 | **配置加载时**执行，运行时不重复执行 | 仅在 `validateOIDCClientSectorIdentifier` 中调用 |
| 运行时回调验证 | Authelia 代码中实现 | 由 **oauthelia2 库内部** 完成 | `NewAuthorizeRequest` 是库函数，Authelia 仅提供 GetRedirectURIs |
| 文件监视器 | 可重载 OIDC 配置 | **仅用于用户数据库**，不涉及 OIDC 配置 | `ProvisionUsersFileWatcher` 只处理 `AuthenticationBackend.File` |
| 动态注册端点 | 结构体字段存在但未启用 | 字段存在但**发现文档未暴露**，无路由实现 | `GetOAuth2WellKnownConfiguration` 未设置 RegistrationEndpoint |
| 配置变更影响 | 变更后已发凭据完全兼容 | 不同配置项影响不同，**secret 变更无兼容** | 密钥轮换功能未启用 |

---

## 6. 关键代码文件索引

| 功能模块 | 文件路径 | 核心函数/结构体 |
|---------|---------|----------------|
| 客户端验证入口 | `internal/configuration/validator/identity_providers.go:22` | `ValidateIdentityProviders` |
| 客户端 ID 校验 | `internal/configuration/validator/identity_providers.go:614-665` | `validateOIDCClients` |
| 单客户端完整校验 | `internal/configuration/validator/identity_providers.go:668-775` | `validateOIDCClient` |
| 回调地址格式校验 | `internal/configuration/validator/identity_providers.go:1253-1287` | `validateOIDCClientRedirectURIs` |
| Sector Identifier 校验 | `internal/configuration/validator/identity_providers.go:887-935` | `validateOIDCClientSectorIdentifier` |
| URI 严格匹配实现 | `internal/oidc/util.go:463-490` | `ValidateSectorIdentifierURI` |
| 已轮换客户端密钥 | `internal/oidc/client.go:159-168` | `GetRotatedClientSecrets` |
| 配置 schema 定义 | `internal/configuration/schema/identity_providers.go:157-239` | `IdentityProvidersOpenIDConnectClient` |
| 路由注册 | `internal/server/handlers.go:392-496` | `RegisterOpenIDConnectRoutes` |
| 发现文档生成 | `internal/oidc/provider.go:38-70` | `GetOAuth2WellKnownConfiguration` |
| 客户端存储 | `internal/oidc/store.go:27-72` | `NewMemoryClientStore` |
| 文件监视器 | `internal/service/file_watcher.go:15-32` | `ProvisionUsersFileWatcher` |
| Token 生命周期默认值 | `internal/configuration/schema/identity_providers.go:242-253` | `DefaultOpenIDConnectConfiguration` |

---

## 7. 总结与安全设计考量

### 7.1 核心设计特点

1. **配置驱动的安全左移**：所有客户端元数据校验在服务启动时完成，运行时仅做最小必要检查
2. **回调地址双重防护**：配置时严格匹配 + 运行时白名单检查，遵循 OAuth 2.0 安全最佳实践
3. **静态配置优先**：仅支持配置文件定义客户端，确保接入方完全可控
4. **密钥轮换预留**：代码层面对密钥轮换有完整设计，但配置层面尚未启用

### 7.2 安全设计亮点

| 安全措施 | 实现位置 | 安全价值 |
|---------|---------|---------|
| 禁止通配符回调 | BestPracticeURIComparisonStrategy | 防止子域名劫持攻击 |
| 精确路径匹配 | 同上 | 防止路径遍历和开放重定向 |
| HTTPS 强制 | `GetRedirectSecureChecker` → `oauthelia2.IsRedirectURISecure` | 防止明文传输授权码 |
| client_id 字符限制 | `reRFC3986Unreserved` 正则 | 防止注入攻击 |
| Sector Identifier 机制 | `ValidateSectorIdentifierURI` | 保护用户隐私，实现 pairwise 主题标识 |

### 7.3 当前限制与改进空间

1. **密钥轮换功能未完全启用**：需要在配置 schema 中增加 `rotated_client_secrets` 字段
2. **缺少动态注册支持**：不支持 RFC7591，无法满足 SaaS 多租户场景
3. **配置变更需重启**：客户端配置修改后必须重启服务，影响可用性
4. **配置变更无主动令牌撤销**：secret 变更后已发 Token 在过期前仍可使用（如果密钥轮换启用可缓解）
5. **无配置版本管理**：无法追溯客户端配置变更历史

### 7.4 生产环境建议

1. 如需密钥轮换：建议在配置 schema 中补充 `rotated_client_secrets` 字段，并修改 `NewClient` 进行赋值
2. 配置变更流程：建立服务滚动重启机制，减少配置变更对用户的影响
3. 敏感配置变更（如 secret、回调地址）：通知用户重新授权，避免访问中断
4. 监控告警：对配置验证失败、回调地址不匹配等安全事件设置告警
