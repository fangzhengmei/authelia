# 授权确认页流程机制调研报告

## 一、概述

本报告对 Authelia 中 OpenID Connect 授权确认页在**显式同意**与**按策略自动放行**两种流程走向的代码协同机制进行深入分析，理清作用域（Scope）计算、Scope 缓存以及二者与会话提升机制的关系。

## 二、三种客户端同意模式

Authelia 为 OIDC 客户端定义了三种同意模式，决定了授权确认页的走向：

| 模式 | 说明 | 场景 |
|------|------|------|
| **Explicit（显式）** | 每次授权都需要用户手动点击同意 | 高安全性要求、首次授权 |
| **Implicit（隐式）** | 自动放行，无需用户交互 | 可信客户端、低敏感度操作 |
| **Pre-configured（预配置）** | 用户预先同意后，后续自动从缓存读取 | 记住用户选择、减少重复操作 |

## 三、核心流程调度机制

### 3.1 主调度函数

`handleOAuth2AuthorizationConsent` 是整个授权同意流程的核心调度入口，执行以下决策链：

1. **会话更新检查**：先调用 `handleOAuth2AuthorizationConsentSessionUpdates` 处理会话刷新
2. **认证级别检查**：计算客户端策略要求的认证级别，与当前会话级别比对
3. **模式路由**：
   - 未认证 → 跳转到登录
   - 认证级别不足 → 触发会话提升流程
   - 认证通过 → 根据客户端配置的同意模式路由到对应处理器

### 3.2 强制显式同意的触发条件

即使客户端配置为非显式模式，以下情况仍会强制使用显式同意：
- 请求参数中包含 `prompt=consent`
- 请求参数中包含 `prompt=login`
- 授权码流程中请求了 `offline`、`offline_access` 或 `authelia.bearer.authz` scope

## 四、显式同意流程（Explicit Mode）

### 4.1 无 consent_id 时的流程（首次请求）

1. 生成新的 `OAuth2ConsentSession` 并保存到存储
2. 检查是否需要重新登录（`RequesterRequiresLogin`）
3. 重定向到授权确认页，用户在页面上选择同意或拒绝

### 4.2 有 consent_id 时的流程（用户提交后）

1. 从存储加载 `OAuth2ConsentSession`
2. 验证 subject 匹配、会话未过期、未被响应过
3. 检查用户是否已授权：
   - 已授权 → 返回 consent 会话，继续授权流程
   - 未授权 → 重定向回确认页

### 4.3 用户提交同意

`OAuth2ConsentPOST` 处理用户的同意/拒绝操作：
1. 验证会话状态和用户身份
2. 检查认证级别是否满足客户端策略要求
3. 调用 `ConsentGrant` 授予 scope 和 claims
4. 支持保存预配置（如果客户端允许且用户勾选）
5. 保存响应结果，重定向完成授权

## 五、隐式自动放行流程（Implicit Mode）

### 5.1 核心逻辑

隐式模式下，系统自动代表用户完成同意操作，无需用户交互。

### 5.2 无 consent_id 时的流程

1. 生成新的 `OAuth2ConsentSession`
2. 直接调用 `ConsentGrantImplicit` 完成授权
3. 保存后返回 consent 会话，继续授权流程

### 5.3 有 consent_id 时的流程

1. 从存储加载会话
2. 验证通过后调用 `ConsentGrantImplicit`
3. 保存并返回

## 六、预配置同意流程（Pre-configured Mode）

### 6.1 预配置缓存结构

`OAuth2ConsentPreConfig` 结构体存储用户的预配置同意信息，包含：
- `ClientID`、`Subject`：客户端和用户标识
- `Scopes`、`Audience`：已同意的作用域和受众
- `GrantedClaims`：已同意的声明
- `ExpiresAt`：过期时间
- `SignatureClaims`：请求声明的签名，用于精确匹配

### 6.2 匹配逻辑

`handleOAuth2AuthorizationConsentModePreConfiguredGetPreConfig` 执行匹配：
1. 加载该用户对该客户端的所有有效预配置
2. 精确匹配 `Scopes` 和 `Audience`
3. 匹配 `Claims` 签名（如果有）
4. 匹配成功则直接使用预配置授权

### 6.3 预配置的创建

用户在显式同意时勾选"记住此选择"后，系统会：
1. 创建 `OAuth2ConsentPreConfig` 记录
2. 设置过期时间（由客户端策略的 `Duration` 决定）
3. 保存到存储供后续使用

## 七、Scope 计算机制

### 7.1 `ConsentGrant` 核心函数

位于 `oidc/session.go:228`，是所有同意模式的 Scope 授予核心：

```go
func ConsentGrant(consent *model.OAuth2ConsentSession, explicit bool, claims []string) {
    consent.GrantAudience()
    consent.GrantClaims(claims)

    if explicit {
        consent.GrantScopes()  // 授予所有请求的 scope
    } else {
        // 隐式模式下排除 offline / offline_access
        for _, scope := range consent.RequestedScopes {
            switch scope {
            case ScopeOffline, ScopeOfflineAccess:
                continue  // 跳过，不授予
            default:
                consent.GrantScope(scope)
            }
        }
    }
}
```

### 7.2 显式与隐式的 Scope 差异

| 模式 | Scope 授予行为 | 可授予 offline_access |
|------|--------------|---------------------|
| 显式 | 授予所有请求的 scope | 是 |
| 隐式 | 排除 offline / offline_access | 否 |
| 预配置 | 使用存储的 scope 配置 | 取决于预配置时的选择 |

**设计意图**：`offline_access` 授予的是长期访问权限（refresh token），出于安全考虑，隐式模式不允许自动授予此类高权限 scope。

### 7.3 `ConsentGrantImplicit` 辅助函数

为隐式模式封装的便捷函数，额外执行：
- 设置 `RespondedAt` 时间戳
- 设置 `Subject`
- 调用 `ConsentGrant(consent, false, claims)`

## 八、Scope 缓存机制

### 8.1 缓存载体

Scope 缓存通过 `OAuth2ConsentPreConfig` 表实现，是持久化存储而非内存缓存。

### 8.2 缓存键的组成

缓存匹配采用复合键策略：
1. **ClientID + Subject**：确定是哪个用户对哪个客户端的预配置
2. **Scopes 精确匹配**：`HasExactGrantedScopes` 要求 scope 列表完全一致
3. **Audience 精确匹配**：`HasExactGrantedAudience` 要求受众列表完全一致
4. **Claims 签名匹配**：`HasClaimsSignature` 对请求的 claims 进行签名比对

### 8.3 缓存生命周期

- 创建：用户在显式同意页勾选"记住此选择"时创建
- 有效期：由客户端配置的 `consent.duration` 决定
- 失效：过期、被撤销、scope/audience/claims 不匹配时失效

## 九、会话提升机制

### 9.1 什么是会话提升

会话提升（Session Elevation）是在用户已有会话的基础上，通过额外的身份验证（如一次性验证码）临时提升会话的安全级别，用于执行高敏感度操作。

### 9.2 提升流程

1. **GET /api/session/elevation**：查询当前提升状态
2. **POST /api/session/elevation**：发起提升请求，系统发送一次性验证码（OTC）到用户邮箱
3. **PUT /api/session/elevation**：用户提交验证码，验证通过后创建提升会话

### 9.3 提升会话的安全约束

- **IP 绑定**：提升会话与创建时的 IP 绑定，IP 变化则失效
- **有效期**：由配置 `identity_validation.elevated_session.elevation_lifespan` 决定
- **一次性使用**：验证码只能使用一次

### 9.4 与会话认证级别的关系

会话提升与会话的认证级别（1FA/2FA）是两个独立但相关的概念：
- **认证级别**：由用户登录时使用的认证方法决定（密码、TOTP、WebAuthn 等）
- **会话提升**：在当前认证级别基础上的额外身份验证

即使是 2FA 认证的会话，执行某些敏感操作（如修改安全设置、授权高权限客户端）时仍可能需要会话提升。

### 9.5 与授权流程的协同

在授权确认流程中，会话提升的触发点：
1. 客户端策略要求特定认证级别
2. 当前会话级别不足时，不直接拒绝，而是：
   - 重定向到登录/2FA 页面提升认证级别
   - 或在 consent 页提示需要进行会话提升
3. 提升完成后，继续原授权流程

## 十、各机制间的协同关系

### 10.1 完整授权流程时序

```
授权请求到达
    ↓
验证客户端、解析请求参数
    ↓
获取用户会话
    ↓
检查是否需要重新登录（prompt=login / max_age）
    ↓
评估客户端策略要求的认证级别
    ↓
当前会话级别是否足够？
    ├─ 否 → 重定向到认证提升流程
    └─ 是 → 检查是否需要显式同意
            ├─ 是（显式模式 / 强制条件）→ 显示同意页
            │       ↓
            │   用户提交同意
            │       ↓
            │   检查是否保存预配置
            │       ↓
            │   授予 Scope（显式模式，含 offline_access）
            └─ 否 → 检查预配置
                    ├─ 找到匹配 → 使用预配置 Scope
                    └─ 未找到 → 授予 Scope（隐式模式，不含 offline_access）
                            ↓
                    生成授权响应，返回客户端
```

### 10.2 Scope 计算与同意模式的关系

同意模式直接决定了 Scope 的计算方式：
- 显式模式 → 完整授予，包括高权限 scope
- 隐式模式 → 受限授予，排除高权限 scope
- 预配置模式 → 使用历史授予记录

### 10.3 Scope 缓存与会话提升的关系

两者是正交的安全机制：
- Scope 缓存解决的是**用户决策复用**问题，减少重复操作
- 会话提升解决的是**身份验证强度**问题，确保操作安全性

即使用户有有效的预配置缓存，在特定情况下（如长时间未操作、敏感客户端）仍可能需要会话提升。

### 10.4 同意模式与预配置缓存的关系

- 只有配置为 `pre-configured` 模式的客户端，用户才能保存预配置
- `explicit` 和 `implicit` 模式不支持预配置缓存
- `implicit` 模式本身就是"自动同意"，无需缓存

## 十一、关键设计决策分析

### 11.1 为什么隐式模式不授予 offline_access？

- offline_access 意味着获得 refresh token，可以长期访问用户资源
- 隐式模式是"无感知"授权，用户没有明确的同意动作
- 出于最小权限和安全考虑，高权限 scope 需要用户明确知情并同意

### 11.2 为什么预配置需要精确匹配 Scope？

- 安全原则：用户同意的是特定的 scope 集合，不能扩大授权范围
- 如果请求的 scope 是预配置的子集，理论上可以匹配，但实现上选择了最保守的精确匹配策略
- 避免 scope  creep（范围蔓延）问题

### 11.3 为什么需要会话提升而不是直接要求 2FA？

- 灵活性：会话提升是临时的，可以针对特定操作
- 用户体验：对于已经 2FA 认证的用户，会话提升提供了更轻量的二次验证方式
- 分级安全：不同敏感度的操作可以要求不同强度的验证

## 十二、总结

Authelia 的授权确认页机制设计体现了分层安全和用户体验的平衡：

1. **多种同意模式**适应不同安全级别的客户端需求
2. **Scope 计算的差异化处理**确保高权限操作得到用户明确授权
3. **预配置缓存机制**在安全可控的前提下减少用户重复操作
4. **会话提升机制**提供了灵活的分级身份验证能力
5. 各机制之间**正交但协同**，共同构成完整的授权安全体系
